# Dependencies
node_modules/

# Auto-generated — never hand-edit
project-management/export/

# Python (Prettier ignores these by default, listed for clarity)
**/*.py
**/*.pyc

# Lockfiles
pnpm-lock.yaml

# Claude Code workspace — format CLAUDE.md only; exclude internal tooling files
.claude/CLAUDE.md.pre-scaffold
.claude/plugins/
.claude/settings.local.json
.claude/worktrees/
# code-review-graph task cards — auto-generated on `install`, referenced by path, never hand-edited
.claude/skills/debug-issue.md
.claude/skills/explore-codebase.md
.claude/skills/refactor-safely.md
.claude/skills/review-changes.md

# Vendored third-party skill definitions — do not reformat
.agents/

# Generated test/lint/audit reports — every reports/ folder under scripts/, not just two.
# An audit run with `--output md` must not fail the format gate it exists to complement;
# markdownlint excludes the same glob for the same reason.
code/src/scripts/**/reports/
code/src/scripts/reports/

# Cargo build output — thousands of generated .json/.rs files, gitignored and machine-
# specific. Rust source itself is formatted by `cargo fmt` via scripts/rust/lint.sh;
# Prettier has no business in either.
code/src/rust/target/

# Django collected static files
code/src/django/staticfiles/

# Relocated marketing CSS + vendored JS (ADR-019) — mirrored from source / third-party; do not reformat
code/src/django/static/css/
code/src/django/static/vendor/

# Django templates — contain template tags that Prettier's HTML parser cannot handle
code/src/django/templates/
code/src/django/apps/**/templates/
# django-components templates (ADR-019) — same reason: Prettier's HTML parser mangles {% %} tags
code/src/django/components/

# Golden-fixture test data — exact-match expected output; must not be reformatted
code/src/django/apps/content/tests/fixtures/

# Misc
.git/
coverage/
htmlcov/
*.min.js
*.min.css
