# code/src/scripts/deployment/reports

Output directory for reports generated by the deployment scripts in `../`.
Currently no deployment scripts exist — this directory is a scaffold placeholder.

## Directory Tree

```text
code/src/scripts/deployment/reports/
├── CONTEXT.md               ← this file
├── .gitignore               ← ignores all generated files; keeps tracked files
└── .gitkeep                 ← keeps this folder present in git when empty
```

## Tracked Files

| File         | Purpose                                                                      |
| ------------ | ---------------------------------------------------------------------------- |
| `.gitignore` | Ignores generated output; keeps `.gitkeep`, itself, and `CONTEXT.md` tracked |
| `.gitkeep`   | Keeps this folder present in git when empty                                  |
| `CONTEXT.md` | This file                                                                    |

Generated reports are never committed.
