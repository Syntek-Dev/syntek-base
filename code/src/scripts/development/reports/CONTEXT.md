# code/src/scripts/development/reports

Reserved output directory for the development scripts in `../`.
Currently no scripts write reports here; the directory exists for future use.

## Directory Tree

```text
code/src/scripts/development/reports/
├── CONTEXT.md               ← this file
├── .gitignore               ← ignores all generated files; keeps tracked files
└── .gitkeep                 ← keeps this folder present in git when empty
```

## Tracked Files

| File         | Purpose                                                                      |
| ------------ | ---------------------------------------------------------------------------- |
| `.gitignore` | Ignores generated output; keeps `.gitkeep`, itself, and `CONTEXT.md` tracked |
| `.gitkeep`   | Keeps this folder present in git when empty                                  |
| `CONTEXT.md` | This file                                                                    |
