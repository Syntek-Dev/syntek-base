---
type: guide
skills: [planner, backend]
model: fable
---

# Clean fixture — the inline flow sequence

Fixture for routing-skills.sh --self-test. Never read as documentation.

Both names resolve to a real skill directory, so this file must produce no finding. It is
the majority form: 246 of the 247 files carrying routing frontmatter write it this way,
which is why a parser that only understood this one looked correct for so long.
