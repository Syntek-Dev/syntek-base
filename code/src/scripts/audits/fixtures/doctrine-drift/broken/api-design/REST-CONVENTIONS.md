# Fixture — the second home

The defect this audit exists for. A guide that is not the owner states the rule again. Both
copies are believed; only one is maintained.

Trips `doctrine-restated`.

```json
{
  "error": {
    "code": "validation_failed",
    "message": "Invalid request."
  }
}
```

It also re-introduces the retired success wrap, which trips `doctrine-banned`.

```json
{
  "data": { "id": "ord_abc123" }
}
```
