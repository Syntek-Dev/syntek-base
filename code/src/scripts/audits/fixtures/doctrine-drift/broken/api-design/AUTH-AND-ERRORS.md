# Fixture — the owner, stating its rule

This file is the registered owner of the `api-error-envelope` claim. It states the rule, which
is correct and must not be a finding on its own.

```json
{
  "error": {
    "code": "validation_failed",
    "message": "The request contained invalid fields."
  }
}
```
