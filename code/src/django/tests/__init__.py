"""Project-level test packages.

Per-app tests live beside their app in ``apps/<app>/tests/``. This package holds the
suites that belong to no single app — currently the browser-level ``e2e`` suite.
"""
