# US001 — Reliability doctrine gets an owning guide, and every pointer reaches it

**Epic:** Retry and Idempotency
**Status:** Open

<!-- FLAGS — one row per gate, and the flag is that gate's entry condition.
     A flag reading N/A means the gate is skipped for this story; every other value means it runs.
     The value is a MANIFEST, not the design: the gate owns the design, may add to this list, and
     the story is updated to match when the gate closes.

     Cut from MAP-RETRY-AND-IDEMPOTENCY.md slice S-01 (nodes N-006, N-007), whose manifest reads
     "QA: manual — docs-length, docs-pairing, doc-references". The QA gate added doctrine-drift
     to that value on 01/09/2026 (QA-PLAN-US001 AC-GAP-6) — the flag is a manifest the gate may
     add to, per docs/planning/CADENCE.md. Twelve rows are N/A because this story ships
     documentation only: no model, no endpoint, no screen, no personal-data path. -->

| Flag       | Value                                                                                  |
| ---------- | -------------------------------------------------------------------------------------- |
| DB         | N/A                                                                                    |
| User Flow  | N/A                                                                                    |
| Brand      | N/A                                                                                    |
| Components | N/A                                                                                    |
| Wireframes | N/A                                                                                    |
| GDPR       | N/A                                                                                    |
| Security   | N/A                                                                                    |
| QA         | manual — `docs-length.sh`, `docs-pairing.sh`, `doc-references.sh`, `doctrine-drift.sh` |
| SEO        | N/A                                                                                    |
| API        | N/A                                                                                    |
| Logging    | N/A                                                                                    |
| Backend    | N/A                                                                                    |
| Frontend   | N/A                                                                                    |

---

## Client Summary

Retry and idempotency rules — what happens when a network call fails, and whether repeating an
operation is safe — are currently spread across three documents that disagree with each other.
This story gives those rules a single home and points every related document at it, so a
developer finds one answer instead of three.

---

## User Story

As a developer building a network-facing surface, I want retry and idempotency doctrine to live in
one named guide that every related document points to, so that I can find the rule governing my
code without reading three guides that contradict each other.

## MoSCoW Priority

**Must Have**

<!-- Blast radius, per the cutting order settled 01/09/2026: this story unblocks
     MAP-RETRY-AND-IDEMPOTENCY S-02 and S-03, and the reliability half of MAP-CAP-POSTURE S-01. -->

## Story Points

5

<!-- A new code/docs/ family with its pair and index rows, two content migrations, one section
     reduction, and three pointer repairs. No code, no migration, no test suite beyond the doc
     gates — moderate, not large. -->

## Dependencies

- **No upstream dependencies.** This is wave 0 of the cutting order — it waits on nothing.
- **This story unblocks:** slices `S-02` (retry doctrine) and `S-03` (idempotency doctrine) on the
  retry-and-idempotency feature map, both of which write into the family this story creates; and
  the **reliability half** of slice `S-01` on the CAP-posture feature map, which owns the
  per-dependency posture register's rule half and cannot land until the family exists. That map's
  architecture half is not blocked by this story.

## Decisions

<!-- The ADRs this story rests on. Each is written at the gate that surfaced its trade-off;
     15-decisions checks the set holds and does not clash. Cite by full repo-relative path
     (ADR-US001-INSTANCE-CITATION-FULL-PATHS-02-09-2026). -->

- `project-management/src/15-DECISIONS/ADR-US001-PROSE-DOCTRINE-VERIFICATION-02-09-2026.md` —
  prose doctrine is verified by a human read-across; `doctrine-drift.sh` runs as a regression
  guard only, because it reads fenced code and this story's doctrine is prose.
- `project-management/src/15-DECISIONS/ADR-US001-INSTANCE-CITATION-FULL-PATHS-02-09-2026.md` —
  PM artefacts cite one another by full repo-relative path, which passes `doc-references.sh`
  through its existence test rather than by dropping the citation.

---

## Acceptance Criteria

Cross-surface retry and idempotency doctrine has exactly one owner — a new reliability family
under `code/docs/` — every rule that moved has left its old home, every rule that stayed is still where it was,
and no document still states the doctrine instead of pointing at it.

```gherkin
Scenario: The family exists and is registered
Given the repository has no code/docs/reliability/ directory
When the story ships
Then code/docs/reliability/ exists with a CONTEXT.md and a CLAUDE.md beside its guide files
And the family's file names are decided in this story rather than inherited from the map,
    with the reasoning recorded in the family's CONTEXT.md
And each guide file states at least one rule that migrated into it
And each new guide file carries routing frontmatter — type: guide, skills:, model:
And an index row for the family exists in the root REFERENCES.md, code/REFERENCES.md,
    code/docs/CONTEXT.md and code/CONTEXT.md
And `bash code/src/scripts/audits/docs-pairing.sh` exits 0

Scenario: The idempotency proof ladder migrates, and the Celery specifics stay
Given TASK-AUTHORING.md states the proof ladder — database constraint, conditional state
    transition, idempotency key for an external effect — under its idempotency section
When the story ships
Then the proof ladder is stated in the reliability family and no longer in TASK-AUTHORING.md
And TASK-AUTHORING.md still states task_acks_late, broker eviction and signature drift
And TASK-AUTHORING.md cites the family for the ladder rather than restating it

Scenario: The retries-and-backoff doctrine bullets migrate, and the class table stays
Given TASK-AUTHORING.md carries a "Retries and backoff" section of doctrine bullets
When the story ships
Then those bullets are stated in the reliability family and no longer in TASK-AUTHORING.md
And the task-surface class table remains in TASK-AUTHORING.md as that surface's expression
And no rule appears in both files

Scenario: Background Jobs and Queues reduces to a monitoring residue
Given performance/API-AND-MONITORING.md carries a "Background Jobs and Queues (Celery)" section
When the story ships
Then that section states only failed-job visibility — this guide's own remit — plus a pointer
    to the reliability family
And every rule it previously carried that duplicates TASK-AUTHORING.md is deleted, not moved
And every rule it previously carried that is neither failed-job monitoring nor a duplicate
    moves to the reliability family rather than being dropped

Scenario: The three pointers reach the new owner
Given PROCESS-MODEL.md, NEGATIVE-SPACE.md and code/docs/CONTEXT.md each point at the old home
When the story ships
Then each of the three points at the reliability family instead
And `bash code/src/scripts/audits/doc-references.sh` exits 0 with no dangling citation

Scenario: The migration loses nothing
Given the three source sections state a countable set of rules before the story begins
When the story ships
Then every rule in that set is accounted for as moved, kept, or deliberately deleted
And the inventory of that accounting is recorded in ../18-TESTS/US001-MANUAL-TESTING.md
And no rule present before the move is absent from every home after it

Scenario: Nothing born over the length limit
Given code/docs/DOCUMENTATION-LENGTH.md sets a 300-line limit and warns from 270
When the story ships
Then every file the story creates is under 270 counted lines at birth
And no file the story edits crosses 270 without a dated allowance
And `bash code/src/scripts/audits/docs-length.sh` reports no new file at or above the warn tier
```

### QA Acceptance Criteria — Manual

<!-- Closed by `../18-TESTS/US001-MANUAL-TESTING.md`. -->

- [ ] `bash code/src/scripts/audits/docs-length.sh` — every new and edited file within limits, and
      no file newly entering the warn tier without a dated allowance
- [ ] `bash code/src/scripts/audits/docs-pairing.sh` — the new directory carries both halves of
      its pair, and neither file carries the other's headings
- [ ] `bash code/src/scripts/audits/doc-references.sh` — every citation resolves, in this tree and
      downstream; no pointer left aiming at a moved rule
- [ ] `bash code/src/scripts/audits/doctrine-drift.sh` — no **new** drift introduced: its three
      registered claims still resolve to one home each. It reads fenced code only, so it does not
      check this story's prose doctrine — see `QA-PLAN-US001` AC-GAP-1, and never report it as
      though it had
- [ ] A human read-across of the reliability family against `TASK-AUTHORING.md` and
      `performance/API-AND-MONITORING.md` — no rule stated in two homes
- [ ] A reader who opens `TASK-AUTHORING.md` cold can reach the migrated rules in one hop
- [ ] A tester other than the author has signed the walk-through off

---

## Tasks

All tasks below map directly to an acceptance criterion above. Mark each complete before raising a PR.

### Documentation Tasks

| Task                                                                                        | Done |
| ------------------------------------------------------------------------------------------- | ---- |
| Capture the rule inventory of the three source sections **before** any rule is moved        | [ ]  |
| Decide the family's file names and record the reasoning in its `CONTEXT.md`                 | [ ]  |
| Create the reliability family under `code/docs/` with its `CONTEXT.md` + `CLAUDE.md` pair   | [ ]  |
| Move the idempotency proof ladder out of `TASK-AUTHORING.md` into the family                | [ ]  |
| Move the _Retries and backoff_ doctrine bullets into the family; leave the class table      | [ ]  |
| Reduce `performance/API-AND-MONITORING.md`'s Celery section to monitoring residue + pointer | [ ]  |
| Repoint `PROCESS-MODEL.md`, `NEGATIVE-SPACE.md` and `code/docs/CONTEXT.md` at the family    | [ ]  |
| Add the family's index rows to the four index surfaces                                      | [ ]  |
| Add routing frontmatter (`type: guide` · `skills:` · `model:`) to each new guide file       | [ ]  |

### QA Tasks — Manual

- [ ] Run the three gates named in the QA flag and record the output in `../18-TESTS/`
- [ ] Re-read each of the three repointed sites in place and confirm the sentence still reads true
- [ ] Record the before/after rule inventory in `../18-TESTS/US001-MANUAL-TESTING.md` and confirm
      it balances
- [ ] Perform the human read-across that `doctrine-drift.sh` cannot do for prose

---

## Verification Checks

Run before raising a PR — via the project scripts under `code/src/scripts/**/*.sh`, never a raw
`python`, `manage.py`, `pytest`, or `docker` call.

<!-- The code-path checks below are marked N/A with a reason rather than deleted: this story ships
     documentation only, and per code/docs/GATE-REPORTING.md a skip is never reported as a pass. -->

- [ ] `bash code/src/scripts/audits/docs-length.sh` passes
- [ ] `bash code/src/scripts/audits/docs-pairing.sh` passes
- [ ] `bash code/src/scripts/audits/doc-references.sh` passes
- [ ] `bash code/src/scripts/audits/doctrine-drift.sh` passes
- [ ] `bash code/src/scripts/syntax/lint.sh` passes (Markdown lint and formatting)
- [ ] `migrate.sh check` — **N/A**, this story touches no model
- [ ] `tests/all.sh --coverage` — **N/A**, this story ships no code path
- [ ] No secrets, debug flags, or hardcoded IDs introduced
- [ ] GDPR / Logging / Security / SEO criteria — **N/A**, each flag reads `N/A`

---

## Definition of Done

- [ ] All acceptance criteria met and verified by a reviewer
- [ ] All tasks checked off
- [ ] All verification checks passed
- [ ] Code reviewed and approved (minimum 1 reviewer)
- [ ] No outstanding TODO or FIXME comments introduced by this story
- [ ] Merged to `main` (or the active release branch)
- [ ] Story status updated to **Completed** in the project board
- [ ] The retry-and-idempotency feature map's slice `S-01` records this story's number in its
      `Story` column
