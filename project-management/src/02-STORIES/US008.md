# US008 — Cookies go host-only under `__Host-` names, the CSRF cookie goes httpOnly, and one guide owns the rule

**Epic:** Subdomain Routing
**Status:** Open

<!-- FLAGS — one row per gate, and the flag is that gate's entry condition.
     A flag reading N/A means the gate is skipped for this story; every other value means it runs.
     The value is a MANIFEST, not the design: the gate owns the design, may add to this list, and
     the story is updated to match when the gate closes.

     DB         — models created / modified            → 04-database-schema
     User Flow  — Yes or N/A                           → 05-user-flow-design
     Brand      — tokens introduced / consumed         → 06-brand-guides
     Components — components introduced / reused       → 07-component-designs
     Wireframes — screens or routes needing a screen   → 08-wireframes
     GDPR       — Yes (personal data) or N/A           → 09-gdpr-compliance
     Security   — concerns (rate-limit, audit-log, …)  → 10-security-checks
     QA         — test types — scenario classes        → 11-qa-checks
     SEO        — affected public pages / routes       → 12-seo-checks
     API        — Ninja endpoints introduced           → 13-api-design
     Logging    — events logged                        → 14-logging-checks
     Backend    — Yes or N/A                           → 19-backend-code
     Frontend   — Public / Admin / Both / N/A          → 21-frontend-code

     Logging has no derived condition: it is its own gate. Every value is filled from the
     feature map's slice row (src/01-FEATURE-MAPS/) at 02-story-creation.

     PROVENANCE. Cut from project-management/src/01-FEATURE-MAPS/MAP-SUBDOMAIN-ROUTING.md slice
     S-02 — "The cookie doctrine lands" — at map line 323: Nodes cell "— (all settled)", flag
     manifest "Security". The map's header reads Frontier open 0, Blocking open 0, Resolved 26,
     and its build-order sentence (:317-318) puts S-02 first, "the only slice with no open nodes,
     and S-01's rule section defers to the doctrine S-02 lands". Thirteen decisions (Q1-Q13) were
     settled at 02-story-creation on 08/09/2026 and confirmed by <%DEVELOPER_NAME%>; none is
     reopened here, and where one is cited below it is cited as settled.

     WIDENED BEYOND THE SLICE (Q1). The slice names two documents — URL-STRATEGY.md:149-153 and
     CRYPTO-AND-DATA.md:122-123. A sweep on 09/09/2026 of the twelve files under code/docs/ that
     contain "cookie" found exactly FIVE that state a cookie's domain, scope, name, path or
     httpOnly: those two, plus api-design/AUTH-STRATEGY.md:67 (the Session cookie row's
     "SESSION_COOKIE_PATH=/" fragment) and the two near-duplicate settings blocks at
     security/AUTH-AND-AUTHZ.md:133-140 and security/OWASP-AND-CHECKLIST.md:44-52. There is no
     sixth owner. The other seven mention cookies and make no scope statement:
     api-design/AUTH-AND-ERRORS.md, api-design/EVENT-TRACKING.md, FRONTEND-CODING-PRINCIPLES.md,
     mcp-server/MOUNTING.md, rendering/CLAUDE.md, visual-design/WEB.md and
     wagtail/ADMIN-AND-PERMISSIONS.md — the last noted below, not edited. Outside code/docs/,
     how-to/src/SCALE-ARCHITECTURE/TOPOLOGY.md:75 makes a session-strategy claim, not a scope
     one, and how-to/src/SERVER-ARCHITECTURE/ has no cookie mention at all.

     THE MAP IS NOT RE-CUT IN THIS CHANGE — <%DEVELOPER_NAME%>, 09/09/2026, amending Q8. The
     disagreement is real and is recorded here rather than left for a reader to rediscover. Four
     resolved-decision "Became" cells — N-004 at :116, N-006 at :118, N-007 at :119 and N-008 at
     :120 — read "build lands on the slice N-019 gates"; the cookie-spine sentence at :211 reads
     "build work for the slice N-019 gates, never this sitting's"; and the closing checklist row
     at :447 reads "graduating with the slice N-019 gates". All six place the cookie build on
     S-01, the slice N-019 gates, while the S-02 row at :323 carries it and the map's own
     build-order sentence says S-02 goes first BECAUSE S-01 defers to what S-02 lands. (:117,
     N-005, is not a target: its cell reads only "The cookie spine below".) Q8's sketch was to
     re-cut all six in the same change as this story, on the precedent of US006, which re-cut
     MAP-SCRIPT-GUARDS in commit 3d149e9. <%DEVELOPER_NAME%>'s ruling on 09/09/2026 is that the
     re-cut ships as a SEPARATE 01-feature-map correction. The reason: 02-story-creation Step 4
     permits exactly one map edit — writing the story number into the Slices row — and states
     that it "does not touch the frontier or the resolved decisions"; the re-cut edits four
     resolved-decision cells, which exceeds that licence. The US006 precedent exists but is one
     story deep, and a re-cut buried in a story commit is invisible to anyone reading the map's
     history for why a resolved decision moved. So this story's one map edit is :323's Story
     column, "—" to "US008"; the six targets are deferred by line, with this reason, and the
     correction is filed as its own change under 01-feature-map. Nothing in this story depends on
     the re-cut having happened.

     THE 02/09/2026 GAPS.md FIGURE IS CORRECTED IN THIS CHANGE — a departure, not a Q-decision.
     That entry's "22 findings" (GAPS.md:454 at ff24084) is re-measured in place beside the two
     new rows, a departure from 22-implementation-documentation's ownership of GAPS.md writes.
     Disclosed here as well as in the Documentation tasks and the Definition of Done, not hidden.

     THE MAP CITES THE OWNER BY BARE FILENAME. "CRYPTO-AND-DATA.md" in the map's S-01 and S-02
     rows is a display form; the file is code/docs/security/CRYPTO-AND-DATA.md, and a story that
     shipped the bare name in backticks would carry a citation that does not resolve. Every path
     in this story is written in full for that reason.

     SECURITY comes from the slice manifest. BACKEND reads Yes because the story edits
     code/src/django/config/settings/base.py, staging.py and production.py — the first story in
     this backlog to touch Python, or anything under code/src/django/, at all. QA is the
     fixture-backed negative-space.sh --self-test that Q7 bought. The other TEN are N/A, GDPR
     included: two strictly-necessary cookies are being hardened, no new personal data is
     collected, no consent surface changes and no retention window moves. No model, no endpoint,
     no screen, no log line, no public page. -->

| Flag       | Value                                                                                                                                                                                                                                          |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DB         | N/A                                                                                                                                                                                                                                            |
| User Flow  | N/A                                                                                                                                                                                                                                            |
| Brand      | N/A                                                                                                                                                                                                                                            |
| Components | N/A                                                                                                                                                                                                                                            |
| Wireframes | N/A                                                                                                                                                                                                                                            |
| GDPR       | N/A                                                                                                                                                                                                                                            |
| Security   | host-only cookie scope (no `Domain`, ever) · `__Host-` names per TLS environment · CSRF cookie httpOnly · session invalidation at the deploy that renames · gate asserts presence and absence                                                  |
| QA         | unit — `negative-space.sh --self-test` over a widened fixture pair, self-test scope repointed with the real one; manual — `doc-references.sh --path code/docs`, the five-document read-across, the advisory dry run, the dev-stack cookie walk |
| SEO        | N/A                                                                                                                                                                                                                                            |
| API        | N/A                                                                                                                                                                                                                                            |
| Logging    | N/A                                                                                                                                                                                                                                            |
| Backend    | Yes                                                                                                                                                                                                                                            |
| Frontend   | N/A                                                                                                                                                                                                                                            |

---

## Client Summary

When someone signs in, their browser holds a small token that keeps them signed in, and a second
one that proves each form they submit is genuinely theirs. This story locks both tokens to the
exact web address that issued them — so a page on any other address, including one this project
might add later, can neither read nor replace them — and stops scripts on the page from reading
the second one at all. It also teaches the project's own automated checks to refuse any future
change that loosens this, and tells an existing project what to look at when it takes the update.

---

## User Story

As a developer deploying `<%PROJECT_SLUG%>` to staging or production, I want the session and CSRF
cookies host-only under `__Host-` names, the CSRF cookie unreadable by script, the rule stated by
exactly one guide, and `audits/negative-space.sh` refusing any settings module that loosens it, so
that no subdomain this project adds later can plant or read a cookie the apex accepts, and no
future edit can reopen that quietly.

## MoSCoW Priority

**Must Have**

<!-- Blast radius (Q12). The tree today mandates the OPPOSITE of the settled rule, in two shipped
     guides. code/docs/URL-STRATEGY.md:149-153 says Phase 2 cookies "must use"
     SESSION_COOKIE_DOMAIN and CSRF_COOKIE_DOMAIN set to the parent domain, and
     code/docs/security/CRYPTO-AND-DATA.md:122-123 says to set an explicit Domain on cookies.
     Both travel into every generated project. A project that follows either sets a cookie the
     whole domain tree accepts — the exact write a subdomain can exploit, source-verified at map
     node N-005. Slice S-01 on the same map ships a rule section that "defers cookie scope to
     CRYPTO-AND-DATA.md"; until this story lands, that deferral would point at a guide saying
     the wrong thing, which is why the map builds S-02 first. -->

## Story Points

8

<!-- Five settings assignments across three modules plus one comment widened; six rows in a
     settings CONTEXT.md table; three prose rewrites, one table cell and two fenced-block edits
     across five guides; three negative-space.sh clauses with the real scope at :92 and the
     self-test scope at :481 moved together and a fixture pair grown on both sides; a copier
     _migrations advisory with its copier.yml entry and its 06-GENERATION.md register row; a
     MINOR version bump at release; two GAPS.md rows; two ADRs; and one map cell.

     Priced against US006's 8 — one _lib helper, six callers, one CI line, a --self-test over a
     fixture tree, two registers, three Markdown files, a map re-cut and two ADRs — and US004's 8,
     five repairs to one 990-line script with one fixture tree. This story has FEWER executable
     lines than either and a WIDER blast radius: it is the first story in the backlog to touch
     Python, code/src/django/config/settings/, a copier migration or the version files at all,
     and the Q7 widening buys an executable surface with a fixture pair, which is the rung US004
     and US006 both sit on. Larger than US007's 5 and US001's 5, which moved prose with no
     executable surface. Not a 13: the only seam is "the settings and the gate" against "the
     guides and the advisory", and project-management/docs/planning/STORIES.md:60-62 rejects a
     layer boundary as a split — a guide that says host-only with no gate behind it is the false
     green this story exists to remove, so the doctrine and its enforcement are one deliverable. -->

## Dependencies

- **No upstream dependencies.** `project-management/src/01-FEATURE-MAPS/MAP-SUBDOMAIN-ROUTING.md`
  records `Frontier open: 0 · Blocking open: 0 · Resolved: 26`; slice S-02's Nodes cell is
  `— (all settled)`; and the map's build order (:317-318) puts S-02 first.
- **This story unblocks S-01 on the same map** — its rule section "defers cookie scope to
  CRYPTO-AND-DATA.md", and the deferral is only true once this story makes it true. It does not
  pre-empt S-01's rule-section filename or content.
- **Shared file with S-04, named at cutting.** S-04 re-casts `code/docs/URL-STRATEGY.md`'s Phase
  2 table (:72-86) as the candidate section; this story rewrites the same file's
  `## CORS and session scope` bullet at :149-153. Different sections, one file: whichever lands
  second re-reads the other's text before editing. The rewrite here presumes no subdomain topology — N-009 leaves
  `admin.` and `portal.` as candidate rows, deliberately undecided.
- **Sprint membership: this story opens SPRINT-05, and the record is opened by
  `03-sprint-planning` in the same change as this story (Q4) — this story edits no sprint record
  and no sprint plan.** Q10 held that this sitting would cut the story only and hand the opening
  on; `03-sprint-planning` ran later on 09/09/2026, the record lands beside this story, and that
  hand-off is spent. The story supersedes a refusal recorded on 07/09/2026 in seven files and ten
  statements, all ten re-measured by `git grep` on 09/09/2026 — every line number in this bullet
  is as it stood at HEAD `ff24084`, before this change corrected three of the ten. The
  strongest is `project-management/src/16-SPRINT-PLANS/04-SPRINT-PLAN-04.md:170-171`: "**A
  SPRINT-05** — declined on 07/09/2026 in favour of the grace above. No fifth record is created,
  and none is implied by this plan." Its siblings are that plan's :126 ("The alternative was a
  SPRINT-05 holding US006 alone"); in `project-management/src/03-SPRINTS/SPRINT-04.md`, :196
  ("No SPRINT-05 is created"), :211 ("a SPRINT-05 holding US006 alone") and :240 ("not opening a
  fifth sprint around one story"); and one each in five further records —
  `project-management/src/03-SPRINTS/SPRINT-03.md:177` ("No SPRINT-05 is opened"),
  `project-management/src/03-SPRINTS/SPRINT-02.md:303-304` ("over a SPRINT-05 holding US006 alone,
  and that record is closed to further admission"),
  `project-management/src/16-SPRINT-PLANS/02-SPRINT-PLAN-02.md:154` ("over a SPRINT-05 holding
  US006 alone"), `project-management/src/02-STORIES/US006.md:202-203` ("over a SPRINT-05 holding
  this story alone, and CLOSED to further admission") and
  `project-management/src/17-STORY-PLANS/06-STORY-PLAN-US005-RETRY-OWNERSHIP-AND-BUDGETS.md:37-38`
  ("taken deliberately over a SPRINT-05 holding US006 alone, and the record is CLOSED to further
  admission"). Seven files, ten statements, none in a file this story edits. All
  ten are dated to, and scoped by, the single 07/09/2026 decision — a choice between grace and a
  one-story sprint for US006 — and none is a standing rule; even the strongest self-limits to
  "this plan". **Three go false or stale the moment SPRINT-05.md exists, and this change corrects
  exactly those three** — by `03-sprint-planning`, not by this story, each superseded in a dated
  comment and none deleted: the bullet under _Won't (this sprint)_ in
  `project-management/src/16-SPRINT-PLANS/04-SPRINT-PLAN-04.md`, which goes false outright; "No
  SPRINT-05 is created" in the 07/09/2026 paragraph under _Notes_ in
  `project-management/src/03-SPRINTS/SPRINT-04.md`; and "No SPRINT-05 is opened" in the re-plan
  paragraph under _Notes_ in `project-management/src/03-SPRINTS/SPRINT-03.md`. The other seven
  record the choice made for US006, which stands, and are untouched. **SPRINT-04 is `**Status:** Planned` (`project-management/src/03-SPRINTS/SPRINT-04.md:21`)**: all four sprint records at HEAD `ff24084`
  are Planned, all seven placed stories Open, and every one of the thirteen boxes under that
  record's Definition of Done (:666-688) is unticked. The record is neither closed nor complete.
  It is closed only **to further admission**, at the 13 / 11 SP grace ceiling (:26-27, :216) — a
  capacity statement, not a lifecycle one, and the two must not be conflated. That cuts **for**
  Q4, not against it: the record refuses to admit an 8 SP story, so a fifth record is the only
  place this one can go. `project-management/src/02-STORIES/US007.md:550` and `:824` already
  assume "the fifth sprint" exists and specify what its Definition of Done copies — evidence for
  a fifth record, not refusals of one. SPRINT-05.md was absent at HEAD `ff24084` and is opened
  in this change, holding this story alone; 05-SPRINT-PLAN-05.md stays absent, deliberately —
  the record is "Opened early" and the plan is written "The moment the sprint fills"
  (`project-management/docs/planning/SPRINTS.md`, the artefact table), and at 8 of 11 SP this
  one has not filled. The ten statements are enumerated here so none is missed; their
  disposition is `03-sprint-planning`'s, and is the one recorded above.
- **Citation-gate regime, named here rather than discovered at implementation.** US004 has not
  landed (SPRINT-03, Open), so the whole-tree `bash code/src/scripts/audits/doc-references.sh`
  is **inherited red**: measured 09/09/2026 at HEAD `ff24084` on a clean tree, exit 1, "253
  citation(s) do not resolve" — 135 `[instance citation]`, 106 `[dangling path]` and 12
  `[template-only citation]`, with no `[plan prefix]` finding anywhere in the tree — over 1035
  files read and 91 exempt by rule. Counted by finding line, never by tag occurrence: the
  script's own legend at the foot of its report prints the words `[template-only citation]` and
  `[plan prefix]` once each as prose, so a bare grep reads 13 and 1 against a stated 253 — the
  miscount this story's first draft carried. Re-measured the same day with this whole change
  staged — `git add -N` over all nine paths, the state it ships in, and named because
  `build_template_only()` reads `git ls-files` and treats an untracked file as shipping: exit 1,
  259 tree-wide — 140 `[instance citation]`, 107 `[dangling path]`, 12 `[template-only citation]`,
  no `[plan prefix]`. The delta is +6 and all six are SPRINT-05.md's — five `[instance citation]`
  on its bare backticked sprint tokens and one `[dangling path]` on a forward reference to a guide
  family US001 creates, the two classes SPRINT-04's own cascade table already carries. This story,
  both ADR-US008 records and the one map edit contribute nothing: each of the three new files
  reads Clean, 0 findings, under `--path`, and `GAPS.md` reads the same nine before and after.
  This story is
  therefore on the **baseline-diff branch** of
  `project-management/src/15-DECISIONS/ADR-US003-CITATION-GATE-BASELINE-DIFF-02-09-2026.md`
  (Option B at :60-66, the Decision at :90-104): the baseline is captured before the first edit,
  read as a diff, and "never reported as the gate passing while the baseline stands". The
  **verification criterion is scoped** (Q6):
  `bash code/src/scripts/audits/doc-references.sh --path code/docs` exits 0 — "Clean — every
  citation resolves." on 09/09/2026 — and must still exit 0 after the five guide edits. The whole-tree figure is never the criterion. The exception
  and its bounds are argued in
  `project-management/src/15-DECISIONS/ADR-US008-SCOPED-CITATION-BASELINE-09-09-2026.md`.
- **Runs beside, not behind.** US007 and US001 (SPRINT-01), US002 and US003 (SPRINT-02), US004
  (SPRINT-03) and US005 and US006 (SPRINT-04) are all parallel work. None of them edits
  `code/src/django/config/settings/`, `code/src/scripts/audits/negative-space.sh`, `copier.yml`
  or any of the five guides; US004 edits `doc-references.sh`, which this story only runs. US002
  owns `code/src/scripts/audits/CONTEXT.md`'s headroom at 298 of 300 code lines as
  `docs-length.sh` measures them, and this story does not touch that file (Q3) — see the named
  cost under the gate scenario.
- **Not in scope, and owned elsewhere:** SPRINT-05 itself — its record is `03-sprint-planning`'s
  and lands in the same change as this story, beside it and not through it; its plan is
  `16-sprint-plans`' and is not written until the sprint fills; re-cutting the map beyond :323 — the six
  targets at :116, :118, :119, :120, :211 and :447 — which is a separate `01-feature-map`
  correction on <%DEVELOPER_NAME%>'s 09/09/2026 ruling; collapsing the two guide
  blocks that restate the deployed settings — the `GAPS.md` row of 09/09/2026 holds it (Q9) —
  and the byte-identical `staging.py:9-22` / `production.py:9-22` block behind them, which no row
  claims and this story names; fixing `template-update.sh`'s preview blindness (the other
  `GAPS.md` row of 09/09/2026, Q11);
  amending, appending to or superseding ADR-US003, which stays exactly as it is (Q13,
  <%DEVELOPER_NAME%> explicit); S-01's rule-section filename and content; this story's own story
  plan (`17-story-plans`); a gate clause over `SameSite`, which `CRYPTO-AND-DATA.md:120-121` owns
  in prose and no script reads today — named as a boundary, not silently omitted; S-03's dev/test
  host hygiene; and S-04's Phase 2 table.

<!-- STORY-PLAN NUMBER RESERVED, 09/09/2026. The prefix on a plan in
     project-management/src/17-STORY-PLANS/ is the story's position in the settled build order
     across the whole backlog, renumbered whenever that order changes
     (project-management/src/17-STORY-PLANS/CLAUDE.md:40-43 owns the rule) — not "next free". The
     run on disk is contiguous 00- to 07-, with US007's plan at 01- and US006's at 07- both
     written on 08/09/2026. US008 builds LAST, behind US006, so 08- is its reserved name over the
     same US008-<DESC> stem the other seven use. The plan is NOT WRITTEN by this workflow —
     02-story-creation stops at the story — and 17-story-plans owns writing it. Recorded here
     rather than in that folder's CONTEXT.md, which ships and may name no per-project story. -->

## Decisions

<!-- The ADRs this story rests on. Each is written at the gate that surfaced its trade-off;
     15-decisions checks the set holds and does not clash. Cited by full repo-relative path. Both
     ADR-US008 records below were written on 09/09/2026, in the same change as this story, and are
     on disk at cutting — the count of two was settled as Q13, and their <DECISION> segments
     differ, as project-management/src/15-DECISIONS/CLAUDE.md:47-50 requires of two records for
     one story on one day. The doctrine itself — host-only cookies, __Host- names per TLS
     environment, the CSRF cookie kept and hardened over CSRF_USE_SESSIONS — earns no record
     HERE: it was argued and settled on the map's cookie spine (N-004 to N-008), and the first
     record below says in its own Follow-on that it "decides nothing about the doctrine itself,
     which the map's cookie spine settled". The reversible decisions the grilling settled — the
     gate host (Q3, Q7), base.py placement (Q2), the collapse declined (Q9) — are edits that can
     be edited back and earn no record, which is the test the template's Decisions comment
     applies. -->

- `project-management/src/15-DECISIONS/ADR-US008-FIRST-MINOR-MIGRATION-KEY-09-09-2026.md` — Q5
  and Q11: the cookie advisory is a print-only `_migrations:` entry keyed `v7.6.0`, the release
  the doctrine ships in, and that is the **first minor key this template has carried** — proved
  on copier's own `Template.migration_tasks()` (a plain PEP 440 parse, no tier inspected) rather
  than assumed from seven `.0.0` examples, and stated deliberately because nothing else in the
  tree says a key may be minor. It fixes the script's shape, its place in `copier.yml`, the
  `06-GENERATION.md` row, the MINOR tier under `VERSIONING-GUIDE.md`, and accepts the preview
  blindness as a `GAPS.md` row rather than a fix (its Option E, declined).
- `project-management/src/15-DECISIONS/ADR-US008-SCOPED-CITATION-BASELINE-09-09-2026.md` — Q6:
  this story's citation criterion is the scoped run,
  `bash code/src/scripts/audits/doc-references.sh --path code/docs`, exit 0, read as the plain
  pass it is; the whole-tree run is executed before the first edit and at close, recorded with
  HEAD, detector hash, count and class, and **never claimed**. A scoped exception to ADR-US003's
  method, not a release from its rule; void the moment this story moves, renames or deletes a
  file under `code/docs/`; retired by the record that retires ADR-US003. It also binds the three
  shipped, scanned files this story edits outside that scope —
  `code/src/scripts/audits/negative-space.sh`, `code/src/django/config/settings/CONTEXT.md` and
  `how-to/src/TEMPLATE-GUIDE/06-GENERATION.md` — to one `--path <file>` run each, exit 0.
- `project-management/src/15-DECISIONS/ADR-US003-CITATION-GATE-BASELINE-DIFF-02-09-2026.md` —
  **binds this story, not only US003**, and is cited here on the US006 and US007 precedent as a
  plain Decisions bullet: it is not amended, appended to or superseded (Q13,
  <%DEVELOPER_NAME%> explicit), and both its supersession fields still read `—`. Its Decision
  applies "to every story in this backlog until the gate is green"; its retirement condition —
  S-06 landing, which is US004 — is unmet; this story is on its baseline-diff branch, and the
  record above is the scoped exception to its method.
- `project-management/src/01-FEATURE-MAPS/MAP-SUBDOMAIN-ROUTING.md` — _The cookie spine_
  (:206-241), the argued record of N-004 to N-008: never set `SESSION_COOKIE_DOMAIN` or
  `CSRF_COOKIE_DOMAIN`; `__Host-` names in `staging.py` and `production.py` beside the
  `SECURE = True` lines they depend on, dev keeping plain names because a browser's storage
  model rejects an insecure `__Host-` cookie; omitting `Domain` stops a subdomain _reading_ the
  apex's cookie and only the prefix stops it _writing_ one the apex accepts (N-005, RFC 6265bis,
  with two named limits); `CSRF_USE_SESSIONS` rejected because an anonymous page rendering
  `{% csrf_token %}` would force a persisted session plus `Vary: Cookie` on a cached marketing
  surface, so the CSRF cookie stays and is hardened with `CSRF_COOKIE_HTTPONLY = True` — in
  `base.py`, because it is the same everywhere (Q2, and independently
  `code/src/django/config/settings/CLAUDE.md:39-41`). Cited as a map, not an ADR, on
  `project-management/src/15-DECISIONS/CLAUDE.md:37-39`'s rule that a map's decision reaches an
  ADR through the slice that becomes a story — and this story's two records are the two
  trade-offs the cut itself raised.
- **No story plan is cited yet.** The `08-` prefix is reserved above; the plan is written at
  `17-story-plans`, and the citation is added then, in the form US006 and US007 use.

---

## Acceptance Criteria

Five settings assignments ship across three modules — `CSRF_COOKIE_HTTPONLY = True` in
`code/src/django/config/settings/base.py`, and `SESSION_COOKIE_NAME` / `CSRF_COOKIE_NAME` under
`__Host-` names in `staging.py` and `production.py` — with the settings `CONTEXT.md` table
updated to match. `code/docs/security/CRYPTO-AND-DATA.md`'s Browser Storage Policy becomes the
sole owner of cookie-scope doctrine, its :122-123 respecified to host-only plus `__Host-`, and the
four other statements in `code/docs/` defer to it or take the one new line. `audits/negative-space.sh`
gains three `[gate: fail]` clauses that assert the doctrine's presence in the modules that must
carry it and its absence everywhere, with the real scope and the self-test scope moved together
and the fixture pair grown on both sides. A copier `_migrations:` advisory keyed `v7.6.0` — the
first minor-keyed migration this template has shipped — tells an updating project what the merge
cannot repair, and the release that carries it is a MINOR bump, 7.5.0 to 7.6.0. Two `GAPS.md`
rows record what this story found and deliberately left. The map's S-02 Story column reads
US008 — made in this change — and nothing else on the map changes.

**The five statements are enumerated once, here, and every count in this story derives from this
table** — no sixth exists, measured 09/09/2026 as the FLAGS provenance records.

| #   | File                                        | Lines    | Today                                                             | Becomes                                                    |
| --- | ------------------------------------------- | -------- | ----------------------------------------------------------------- | ---------------------------------------------------------- |
| 1   | `code/docs/security/CRYPTO-AND-DATA.md`     | :122-123 | "Set an explicit `Path` and `Domain` on cookies ..."              | **The owner** — host-only, `__Host-`, gate marker inline   |
| 2   | `code/docs/URL-STRATEGY.md`                 | :149-153 | Phase 2 "must use" `SESSION_COOKIE_DOMAIN` / `CSRF_COOKIE_DOMAIN` | Defers to 1; the CORS allowlist sentence survives verbatim |
| 3   | `code/docs/api-design/AUTH-STRATEGY.md`     | :67      | Session cookie row ends "(`SESSION_COOKIE_PATH=/`)"               | Defers to 1                                                |
| 4   | `code/docs/security/AUTH-AND-AUTHZ.md`      | :133-140 | Eight settings, no `CSRF_COOKIE_HTTPONLY`                         | Gains `CSRF_COOKIE_HTTPONLY = True` beside :138            |
| 5   | `code/docs/security/OWASP-AND-CHECKLIST.md` | :44-52   | The same eight plus `SECURE_BROWSER_XSS_FILTER` (:52)             | Gains `CSRF_COOKIE_HTTPONLY = True` beside :49             |

```gherkin
Scenario: Five assignments ship across three modules, split by the rule the folder already states
Given code/src/django/config/settings/ holds exactly eight tracked files — __init__.py, base.py,
    dev.py, production.py, staging.py, test.py, CONTEXT.md, CLAUDE.md — last touched 23/08/2026
    in c09a189 and untouched by the commit batch of 07-08/09/2026
And base.py:159-160 carry SESSION_COOKIE_HTTPONLY = True and SESSION_COOKIE_SAMESITE = "Lax", and
    :161-162 is a comment explaining that SESSION_COOKIE_SECURE is intentionally absent because
    dev serves over HTTP
And staging.py:9-22 and production.py:9-22 are byte-identical — DEBUG, ALLOWED_HOSTS,
    CSRF_TRUSTED_ORIGINS, SECURE_PROXY_SSL_HEADER, SECURE_SSL_REDIRECT, SESSION_COOKIE_SECURE = True
    at :21 and CSRF_COOKIE_SECURE = True at :22 — fourteen lines, not two
And dev.py and test.py carry no cookie setting at all, which is what N-006's "dev keeps plain
    names" already requires
And a repo-wide search for __Host, SESSION_COOKIE_NAME, CSRF_COOKIE_NAME, SESSION_COOKIE_DOMAIN,
    CSRF_COOKIE_DOMAIN, SESSION_COOKIE_PATH, CSRF_COOKIE_PATH, CSRF_COOKIE_SAMESITE and
    CSRF_COOKIE_HTTPONLY returns zero hits in code/src on 09/09/2026 — nothing is half-built
When the story ships
Then base.py gains CSRF_COOKIE_HTTPONLY = True directly beside :159-160, once, and no environment
    module re-assigns it — code/src/django/config/settings/CLAUDE.md:39-41: "A value that differs
    per environment goes in the environment module; a shared one in base.py. An override that
    happens to match the base value is a future divergence nobody will notice." That rule
    justifies both halves of Q2 on its own, and this story cites it rather than re-arguing it
And staging.py and production.py each gain SESSION_COOKIE_NAME = "__Host-sessionid" and
    CSRF_COOKIE_NAME = "__Host-csrftoken" directly beside the SECURE = True lines at :21-22 —
    Django's default names, prefixed — identical in both modules, pasted twice on purpose (Q9),
    with the duplicated block growing from fourteen lines to sixteen; that growth is named here
    and claimed by no GAPS.md row, the row of 09/09/2026 holding the two guide blocks instead
And no module anywhere assigns SESSION_COOKIE_DOMAIN, CSRF_COOKIE_DOMAIN, SESSION_COOKIE_PATH or
    CSRF_COOKIE_PATH: Django's defaults — no domain, and a path of "/" — are exactly what a
    __Host- name requires, so the ABSENCE is the rule and is asserted as one below
And dev.py and test.py are byte-identical to HEAD ff24084 after the change
And base.py:161-162's comment is widened to name CSRF_COOKIE_SECURE alongside
    SESSION_COOKIE_SECURE — it is equally absent from base.py and equally True in both
    environment modules, and a CSRF line landing beside a comment that names only the session
    cookie makes the omission conspicuous; the comment is prose and the gate's matches are
    assignment-anchored, so naming the setting there is neither a finding nor a pass
And code/src/django/config/settings/CONTEXT.md's Critical Settings table (:33-47) gains rows on
    the :47 SESSION_COOKIE_SECURE precedent, which already documents a per-environment value as
    "_absent_" from base.py: CSRF_COOKIE_HTTPONLY (base.py, True); SESSION_COOKIE_NAME and
    CSRF_COOKIE_NAME (_absent_ — staging.py and production.py set the __Host- names, dev
    inherits Django's plain names because a __Host- cookie cannot be insecure);
    CSRF_COOKIE_SECURE (_absent_, the same shape as :47); and SESSION_COOKIE_HTTPONLY and
    SESSION_COOKIE_SAMESITE, which live at base.py:159-160 and were never documented — added so
    the CSRF row does not sit beside an asymmetry it makes visible.
    code/src/django/config/settings/CLAUDE.md:23-24 makes the table update compulsory
And the Module Map's staging.py cell (:27) reads "secure, __Host--named cookies" where it read
    "secure cookies", and the production.py cell (:28) — "As staging, plus HSTS", which carries no
    cookie phrase to replace — inherits that wording unchanged, so the map names the divergence
    that now exists without stating it twice

Scenario: The doctrine has one owner, and the other four statements defer to it
Given code/docs/security/CRYPTO-AND-DATA.md:122-123 — the fourth of five bullets under "### Rules"
    (:113) inside "## Browser Storage Policy" (:91) — reads "Set an explicit Path and Domain on
    cookies to prevent them being sent more broadly than intended", which mandates the exact
    opposite of the settled rule
And its neighbours constrain the rewrite: :120-121 owns SameSite (Lax minimum, Strict where a
    cookie need not cross a navigation) and :124-125 owns Secure ("There is no valid reason for a
    cookie to be sent over HTTP in production")
And sole ownership is already structurally supported: three inbound links point at the Browser
    Storage Policy — code/docs/api-design/AUTH-AND-ERRORS.md:27, code/docs/api-design/AUTH-STRATEGY.md:134
    and :178 — and both index files name it, code/docs/SECURITY.md:23 and
    code/docs/security/CONTEXT.md:13
When the story ships
Then :122-123 is respecified: cookies are HOST-ONLY — Domain is never set, so a cookie is sent
    only to the host that issued it — and the session and CSRF cookies take __Host- prefixed
    names in every environment that serves TLS, because the prefix is what stops a subdomain
    WRITING a cookie the apex accepts, where omitting Domain only stops it READING one (N-005).
    The prefix's three preconditions are stated with it — Secure, which :124-125 already
    mandates and the bullet now leans on rather than restates; Path "/", the only surviving
    sense of the old Path clause; and no Domain — and the two limits N-005 named travel with the
    rule: protection is per cookie name, and a browser without prefix support accepts a prefixed
    cookie unconditionally, so the prefix is defence in depth over the settings, never a
    substitute for them
And the bullet does not restate :120-121 or :124-125, and neither of those moves
And the rule carries the gate marker inline — **[gate: fail]** with the clause names in
    parentheses — on the shape code/docs/NEGATIVE-SPACE.md:222-224 uses for
    request-id-middleware-absent, stated where its subject lives exactly as
    code/docs/MOBILE-CODING-PRINCIPLES.md:48 does for ts-flags-loosened and
    code/docs/mcp-server/TOOL-DESIGN.md:119 for mcp-masking-off. It is NOT a row in
    how-to/src/INVARIANTS.md, whose mechanism vocabulary at :19 — db-constraint, service-guard,
    client-guard, both — fits no settings clause and whose three tables are model-, service- and
    screen-shaped; and it is NOT a bullet in NEGATIVE-SPACE.md's list at :148-157, which
    enumerates the register clauses and not the surface ones
And code/docs/URL-STRATEGY.md:149-153 is rewritten to DEFER: the "must use SESSION_COOKIE_DOMAIN /
    CSRF_COOKIE_DOMAIN" mandate goes, replaced by one sentence routing cookie scope to
    code/docs/security/CRYPTO-AND-DATA.md's Browser Storage Policy and stating that a second
    host, if one is ever routed, gets its own host-only cookies rather than a shared one. THREE
    THINGS SURVIVE VERBATIM: "Admin and portal sessions remain separate — a portal login does
    not grant admin access"; "Implement subdomain migration only after explicit security
    review"; and the :152-153 sentence "Where a real cross-origin caller exists,
    CORS_ALLOWED_ORIGINS must be an explicit allowlist — never * in production", which is a
    .claude/CLAUDE.md Section 6 non-negotiable and must not be lost to a rewrite aimed at the
    cookie clause above it
And :147-148 — the Phase 1 bullet, ONE wrapped bullet that quoting :147 alone truncates — is left
    alone, because "cookies are SameSite=Lax, and no CORS configuration is needed between the
    admin and portal surfaces" is true under host-only cookies
And :154-156, the Edge contract bullet, is untouched, and the rewritten bullet makes no cookie
    claim that the untouched Phase 2 section at :72-86 and its table at :78-82 contradict — that
    section makes no cookie claim of its own, and its re-cast is S-04's
And code/docs/api-design/AUTH-STRATEGY.md:67's final cell — the Session cookie row of the
    five-column table under "## The schemes and when to use each" (:63; header :65, separator
    :66, data rows :67-71) — drops the "(SESSION_COOKIE_PATH=/)" fragment, the tree's second
    cookie-scope statement, and defers in its place: "cookie scope:
    ../security/CRYPTO-AND-DATA.md, Browser Storage Policy". The cell's width changes;
    Prettier and markdownlint normalise the column padding and it is never hand-tuned. The file's
    other cookie mentions, :134 and :178 among them, already defer correctly and are out of scope
And code/docs/security/AUTH-AND-AUTHZ.md:133-140 gains CSRF_COOKIE_HTTPONLY = True beside
    CSRF_COOKIE_SECURE at :138, and code/docs/security/OWASP-AND-CHECKLIST.md:44-52 gains the
    same line beside its CSRF_COOKIE_SECURE at :49 — BOTH blocks (Q9). The two are
    near-duplicates in two respects, not one: a diff of the two bodies returns exactly one hunk,
    "8a9 > SECURE_BROWSER_XSS_FILTER = True", so the eight shared settings are byte-identical
    and identically ordered, and OWASP's block alone carries the "# settings/production.py"
    comment at :43. Neither block gains the __Host- names: the names are per-environment values,
    the blocks are the shared deployed baseline, and the owner is one file — each block instead
    gains one sentence beneath it routing cookie names and scope to the Browser Storage Policy.
    That OWASP's "# settings/production.py" comment names one module for a block two modules
    carry is recorded in the GAPS.md row rather than corrected here; that the new line lives in
    base.py rather than either environment module is stated here, because each block's heading
    claims only that the setting is enabled in staging and production, which a base.py value
    satisfies
And code/docs/wagtail/ADMIN-AND-PERMISSIONS.md:47-50 is NOTED, NOT EDITED: "A session obtained at
    the CMS login is a session everywhere" names no setting, so it is not a scope statement; it
    is true under host-only cookies for as long as every surface shares one host, and it would
    stop being true on a subdomain split — which is the topology N-009 leaves undecided. CMS-only
    doc; the note goes in the manual-testing record beside the sweep
And after the five edits, bash code/src/scripts/audits/doc-references.sh --path code/docs exits 0
    with "Clean — every citation resolves." — the criterion (Q6), not the whole tree

Scenario: The gate asserts presence AND absence, and its self-test proves the same shape as the real run
Given code/src/scripts/audits/negative-space.sh is 624 lines: SETTINGS_FILE at :92 names base.py
    alone; run_middleware_clause at :405-416 is the clause shape to copy, with its skip idiom at
    :407 and the fail/skip helpers at :209-211; --path is refused outright at :182 and
    --self-test is the only sanctioned scope change; and the script's own header at :59-65 names
    "accepting a scope and honouring none of it" as the defect it refuses at the parser
And point_scopes_at at :481 sets SETTINGS_FILE="$root/settings.py" — a SINGLE file — so widening
    :92 without repointing :481 coherently makes the self-test prove a different shape than the
    real run, which is the header's own defect wearing the self-test's clothes
And the cookie settings are deliberately split across three modules, so a clause scoped to
    base.py cannot see SECURE or the names at all, and a naive widening to the whole settings
    directory fires falsely on dev.py and test.py, which correctly carry nothing
And the self-test (:488 onward) points every scope at fixtures/negative-space/broken, runs every
    clause, harvests the clause names with cut -d: -f1, repoints at clean/, runs again, and
    asserts that every name in the eleven-entry EXPECTED array at :506-508 appeared in the broken
    set and that clean produced nothing; a missing fixture dies at exit 2 (:492-493), never a
    silent pass. Fixtures are flat mini-trees — nine files under broken/, thirteen under clean/
    — and the names INVARIANTS.md, settings.py, tsconfig.json, mcp.py and asgi.py are FIXED by
    point_scopes_at while everything else is found by recursive scan
When the story ships
Then the script gains a settings-DIRECTORY scope beside SETTINGS_FILE, and :92 and :481 move
    TOGETHER: the real run points it at code/src/django/config/settings/, the self-test at a
    settings/ directory under $root, and the module names inside it — base.py, staging.py,
    production.py, dev.py, test.py — are fixed by point_scopes_at exactly as settings.py is.
    SETTINGS_FILE and run_middleware_clause stay as they are
And three [gate: fail] clauses are added, named apart the way key-unraised, key-unregistered and
    key-duplicated are, because the three ways to break the doctrine are three different repairs:
      csrf-cookie-httponly-absent  base.py does not assign CSRF_COOKIE_HTTPONLY = True
      cookie-host-prefix-absent    staging.py or production.py lacks a SESSION_COOKIE_NAME or
                                   CSRF_COOKIE_NAME beginning __Host-, or lacks the
                                   SESSION_COOKIE_SECURE = True / CSRF_COOKIE_SECURE = True the
                                   prefix depends on, in the same module
      cookie-scope-widened         any module under the directory assigns SESSION_COOKIE_DOMAIN
                                   or CSRF_COOKIE_DOMAIN, or assigns SESSION_COOKIE_PATH or
                                   CSRF_COOKIE_PATH to anything but "/"
And PRESENCE is asserted per named module — base.py for the first clause, staging.py and
    production.py for the second — and ABSENCE over every module in the directory, which is why
    dev.py and test.py, carrying no cookie setting, can never fire either way
And every match is assignment-anchored at the start of a line, so a setting named in a comment
    — the widened base.py:161-162 among them — is neither a finding nor a pass, and a commented-out
    assignment is absent
And each clause skips with a note when the module it reads is absent, on the :407 idiom — an
    absent SURFACE, reported as such, never an absent tool reported as clean
    (code/docs/GATE-REPORTING.md Section 2)
And the EXPECTED array grows from eleven entries to fourteen; broken/ gains a settings/ tree that
    trips all three clauses and clean/ one that trips none; the missing-fixture exit 2 is unchanged
And the clause is proved to discriminate by a pre-change comparison, recorded: the NEW script run
    over the settings modules as they stand at HEAD ff24084 reports all three clauses, and run
    over the modules after the first scenario lands reports none. There is no comparison the
    other way round — the OLD script has no clause for a fixture to fail against, so the
    "fixture fails against the pre-change script" form US004 uses does not apply here, and
    saying so is what stops it being claimed
And bash code/src/scripts/audits/negative-space.sh --self-test exits 0, and the unscoped run on
    this tree exits 0 after the settings land, with none of the three clauses in the skip notes
And the SameSite attribute is NOT asserted — code/docs/security/CRYPTO-AND-DATA.md:120-121 owns it
    in prose, Django's default of Lax satisfies it, and widening the clause to it is a later
    story; the boundary is stated so it is read rather than assumed
And .github/workflows/audit-negative-space.yml needs no edit: its path filters (:34-39 on push,
    :43-48 on pull request) already include code/src/django/**, the script and its fixtures, and
    the job runs --self-test first (:60) and the audit second (:63)
And code/src/scripts/audits/CONTEXT.md is NOT edited (Q3). Its negative-space.sh inventory cell at
    :170 enumerates the clauses and says "Twelve [gate: fail]"; after this story there are
    fifteen and the cell under-counts by three. That is a NAMED COST of Q3, not an oversight:
    the file's docs-length allowance at :8 expires 01/12/2026, US002 owns its two lines of
    headroom, and the repair — extending the existing cell in place, which costs no counted line
    however much prose it gains — belongs to whoever next edits that file, with the register at
    :170 the first thing they touch

Scenario: HTMX keeps its CSRF token, and nothing reads the cookie
Given Django's settings reference states that with CSRF_COOKIE_HTTPONLY enabled, JavaScript must
    retrieve the CSRF token from a hidden form input instead of the cookie, and its CSRF how-to
    gives the DOM read for exactly that case
And the tree's pattern is already template-injected: <body hx-headers='{"X-CSRFToken":
    "{{ csrf_token }}"}'> at code/docs/rendering/PITFALLS-AND-EXAMPLES.md:100 and {% csrf_token %}
    in every form (code/docs/rendering/TEMPLATES-AND-INTERACTIVITY.md:88), which the map recorded
    at :216-218 as "zero cookie reads anywhere; the doctrine pattern is template-injected
    hx-headers"
And that measurement was repeated on 09/09/2026: no document.cookie, getCookie or csrftoken read
    exists in any .js, .html or .py file under code/src/django
When the story ships
Then CSRF_COOKIE_HTTPONLY = True ships with no template and no JavaScript change
And the dev-stack walk confirms it: with the stack up, a login at /control/ succeeds, a form POST
    succeeds, and an HTMX write carrying the template-injected header succeeds; the browser shows
    the csrftoken cookie flagged HttpOnly under dev's plain names; no console error
And Django's CSRF middleware reads the cookie by CSRF_COOKIE_NAME and the session middleware by
    SESSION_COOKIE_NAME, so the __Host- names in staging and production change nothing else

Scenario: The advisory ships as the first minor-keyed migration, and the version moves with it
Given copier.yml:814 opens _migrations: with EIGHT entries: seven MAJOR-keyed — v7.0.0 declared
    first for ordering, then v2.0.0 through v6.0.0, with v4.0.0 keyed TWICE (:852 and :873, the
    second pointing at v5.0.0-git-guide-split.sh for the reason its comment at :856-872 argues)
    — then the unversioned rm -rf .copier at :929-930 which must stay LAST
And Template.migration_tasks() in copier gates each entry on
    from_template.version < current <= self.version, where current is a plain PEP 440 parse of
    the entry's version with no major restriction — in copier 9.17.2 the predicate sits at
    _template.py line 457 and the continue at 458 (line 442 in 9.17.0 and 9.17.1, so a one-line
    quote merges two); Template.version derives from git tags through dunamai, and this
    repository carries 72 tags including the minors v7.1.0 to v7.5.0, so a v7.6.0 key resolves
    and fires
And the repository pins no copier version: _min_copier_version: "9.6.0" at copier.yml:13 is a
    floor, and both live invocations are bare uvx copier —
    .github/workflows/audit-template.yml:154 and
    code/src/scripts/development/template-update.sh:114 — a hazard
    project-management/src/01-FEATURE-MAPS/MAP-GATE-PARITY.md:196-199 already records and this
    story does not repair
And the six scripts under .copier/migrations/ fix the convention, fully specified by
    v3.0.0-self-authored-agents.sh (91 lines): filename v<VERSION>-<kebab-slug>.sh;
    set -euo pipefail; a long # header covering WHAT CHANGED, WHY AN UPDATE CANNOT REPAIR IT,
    WHY IT ADVISES RATHER THAN ACTS, the working directory and the exit codes; an early exit 0
    when the target is absent; the banner printf '\n▸ vX.Y.Z migration — <subject>\n\n'; a
    guidance heredoc ending in a bash code/src/scripts/audits/... proof step; and exit 0 ALWAYS.
    Per v5.0.0-git-guide-split.sh's own header the filename records the release the advisory was
    WRITTEN FOR, not every key it fires on
When the story ships
Then .copier/migrations/v7.6.0-host-only-cookies.sh exists on that convention, advisory only,
    and it reports rather than edits: every module under code/src/django/config/settings/ that
    still assigns SESSION_COOKIE_DOMAIN or CSRF_COOKIE_DOMAIN, with file and line — the project
    that followed the old URL-STRATEGY Phase 2 mandate, where copier's three-way merge keeps the
    developer's Domain line beside the template's new __Host- name without a conflict, every
    browser then rejects the cookie by the prefix's own definition, and every login in that
    environment fails behind a success message. It exits 0 early when
    code/src/django/config/settings/ is absent. Its guidance names the two things that are the
    operator's alone — the rename invalidates every live session and every open CSRF token at
    the first deploy, so WHEN is a decision; and anything matching a cookie by name (an edge
    rule, a monitor, a test, a script reading the CSRF cookie that CSRF_COOKIE_HTTPONLY = True
    now blinds) must be repointed — and ends in the proof step
    bash code/src/scripts/audits/negative-space.sh, the gate this same story widens. It prints
    file paths, line numbers and setting names, never values. A project holding a _DOMAIN line
    DELIBERATELY — the N-005 case — gets a report it must judge and dismiss; the advisory says so
    rather than deciding, which is why it advises and does not act
And copier.yml gains the v7.6.0 entry immediately after the v6.0.0 entry (:917-919), before the
    comment block at :921-928, with the same command: and when: lines the seven existing
    versioned entries carry — resolved through the template clone's src_path and gated to the
    after stage; the
    delimiter pair is not spelled here because .github/scripts/check-template-tokens.sh scans
    this folder — leaving rm -rf .copier last
And the entry's comment says, deliberately and not in passing, that this is the FIRST
    minor-keyed migration this template has shipped: the predicate is a plain version
    comparison, and a doctrine reversal that breaks a developer-written setting is the case the
    mechanism exists for whatever the increment — the comment is what stops the next reader
    concluding that only a MAJOR may carry one
And how-to/src/TEMPLATE-GUIDE/06-GENERATION.md's per-version register (:201-209) gains a v7.6.0
    row at :209's position, BEFORE the _(every)_ row, because the table is declaration order and
    not version order (:211-212); marked **Advisory.**; and its length is NOT a gate figure:
    docs-length.sh never reads the file — is_instructional() binds only a CONTEXT.md or CLAUDE.md
    basename, or a path carrying a docs, workflows or .claude segment, and
    how-to/src/TEMPLATE-GUIDE/ carries none — so the 300-line limit and the 270 ratchet do not
    apply to it, no allowance is needed, and none could be claimed
And VERSION, CHANGELOG.md, VERSION-HISTORY.md and RELEASES.md — all at 7.5.0 on 09/09/2026 — move
    to 7.6.0 through the version skill at release, a MINOR bump under
    project-management/docs/VERSIONING-GUIDE.md:76 ("new features, backwards-compatible"): the
    template's contract gains a gate clause and a doctrine; the cookie rename is a deploy
    consequence the advisory carries, not a break in anything a project calls. Cited here so
    the migration key and the tag agree, because a key that never matches a tag never fires
And project-management/docs/git/MIGRATION-GATES.md is NOT cited anywhere in this change: it
    sounds governing and is not — it covers database migration review gates and contains zero
    occurrences of "copier"

Scenario: The map's one permitted edit is made, and the deferred re-cut is named
Given 02-story-creation Step 4 (project-management/workflows/02-story-creation/STEPS.md:90-96)
    permits one edit to a wayfinder artefact — the allocated number into the Slices row's Story
    column — and states it "does not touch the frontier or the resolved decisions"
And the map was untouched by the recent commit batch — last commit 82ec176 on 02/09/2026 — so
    every line cited here still lands; the map is 455 lines
When the story ships
Then project-management/src/01-FEATURE-MAPS/MAP-SUBDOMAIN-ROUTING.md:323's Story column reads
    US008 and nothing else in the map changes — made on 09/09/2026 in the same change as this
    story, and the map's diff is that one cell
And the six re-cut targets — :116, :118, :119, :120, :211 and :447, each placing the cookie build
    on the slice N-019 gates — are NOT edited by this story, for the reason the FLAGS provenance
    records, and the separate 01-feature-map correction is filed with that list

Scenario: Two GAPS.md rows record what was found and left, one stale figure is corrected, and the cost to the citation gate is stated
Given GAPS.md's Format block (:228-240), with the live example at :296-313, fixes the entry shape:
    an H2 "## DD/MM/YYYY — <title>", a blank line, then **Type:** / **Summary:** /
    **Blocked by / Action:** on consecutive lines, bracketed by bare --- separators
And before this change neither candidate row existed — GAPS.md never named AUTH-AND-AUTHZ.md or
    OWASP-AND-CHECKLIST.md, and "template-update.sh" appeared nowhere in GAPS.md or DEFERRED.md
    (seventeen lines of header, zero rows) — and the file ended in prose at 467 lines with no
    trailing ---
And GAPS.md's entry of 02/09/2026 carried one stale figure, "22 findings" at :454 — the
    whole-run total as it stood that day, and the only figure in the entry that had moved
And nine of the 253 whole-tree doc-references.sh findings on 09/09/2026 are GAPS.md's own
    citations of PM src/ artefacts (:418-456 at HEAD ff24084, before this change's lines land),
    because GAPS.md SHIPS and those artefacts do not
When the story ships
Then GAPS.md carries two entries in that shape, written 09/09/2026 in the same change as this
    story. "09/09/2026 — the staging/production security-settings block ships twice and has
    already drifted" (Type: Active gap; class D, split doctrine): the two guide blocks, their
    one-hunk diff, the "# settings/production.py" comment one fence carries and the other does
    not, the ninth setting SECURE_BROWSER_XSS_FILTER that the installed Django no longer
    declares, and the fact that neither block describes what
    code/src/django/config/settings/production.py:19-28 actually assigns; it records that this
    story pastes CSRF_COOKIE_HTTPONLY = True into BOTH blocks rather than collapsing them (Q9),
    and holds the collapse. And "09/09/2026 — a print-only _migrations: advisory is invisible in
    the template-update.sh preview" (Type: Active gap; class B, false green): the wholesale
    redirect at :143-144, the failure-only tail at :148-151, the cleanup() at :122, trapped on
    EXIT at :123, that unlinks the log, "Preview only — your project is unchanged" at :275
    printed over an update whose
    migration had something to say, and the unredirected --apply at :281-282 that prints the
    advice exactly once, after the decision it was written to inform; the story accepts the
    blindness and does not fix the script (Q11)
And the 02/09/2026 entry's stale figure is corrected in place, in the same change — 253 at
    ff24084, by the classes the gate itself reports — with the superseded sentence kept verbatim
    in a dated HTML comment beneath the entry, so the correction is visible rather than silent
And the byte-identical staging.py:9-22 / production.py:9-22 block behind the two guide blocks is
    named in this story's first scenario and claimed by no row: the guide row cites production.py
    as what actually runs, and a settings-module refactor is not this story's
And neither row adds to the whole-tree doc-references.sh count, measured 09/09/2026 both ways:
    each carries the doc-references template-only marker on the one citation the gate would
    otherwise report — project-management/src/01-FEATURE-MAPS/MAP-SUBDOMAIN-ROUTING.md in the
    first row, .copier/migrations/ in the second, which cites no PM src/ path at all — so --path
    GAPS.md reports the same nine findings, by the same nine citations, at HEAD ff24084 and with
    the two rows in place. This file adds nothing either once it is in the index — Clean, 0
    findings under --path, measured the same day — and nor do the two ADR-US008 records, Clean
    at 0 each, or the one map edit. That is the sense in which GAPS.md says US008's own change
    adds nothing, and it is true of this story's artefacts only. The COMMIT is a different
    statement: with the whole change staged (git add -N, all nine paths) the whole tree reads
    259 against the 253 baseline, and the +6 is SPRINT-05.md alone — five instance citations on
    its bare backticked sprint tokens and one dangling path on a forward reference to a guide
    family US001 creates — the record 03-sprint-planning opens beside this story, not through
    it. The two statements are kept distinct here so this file and GAPS.md do not contradict
    each other; whatever the figure reads at close is attributed by citer in the manual-testing
    record, and it is never netted against the baseline as though it were pre-existing
```

### Backend Acceptance Criteria

<!-- Backend: Yes — the story edits three settings modules. The template's service-layer rows are
     replaced, not deleted: there is no service function, signal, task or user-supplied ID here,
     and the checkbox that applies to configuration is stated in its place. -->

- [ ] `code/src/django/config/settings/base.py` assigns `CSRF_COOKIE_HTTPONLY = True` exactly
      once, beside :159-160, and no environment module re-assigns it
- [ ] `staging.py` and `production.py` each assign `SESSION_COOKIE_NAME = "__Host-sessionid"` and
      `CSRF_COOKIE_NAME = "__Host-csrftoken"` beside their `SECURE = True` lines at :21-22, and no
      other module assigns either name
- [ ] No module under `code/src/django/config/settings/` assigns `SESSION_COOKIE_DOMAIN`,
      `CSRF_COOKIE_DOMAIN`, `SESSION_COOKIE_PATH` or `CSRF_COOKIE_PATH`
- [ ] `dev.py` and `test.py` are byte-identical to HEAD `ff24084`
- [ ] `bash code/src/scripts/tests/all.sh` is green — the test settings module still imports and
      every existing test passes. **No new Python test is written**, deliberately: the gate clause
      is the invariant's one named enforcement point (`code/docs/NEGATIVE-SPACE.md`, the governing
      principle), and a pytest assertion over the same five values would be a second enforcer of
      the same rule, which is the shape that guide forbids
- [ ] `bash code/src/scripts/syntax/lint.sh --file-type python --path code/src/django/config/settings`
      and `bash code/src/scripts/syntax/check.sh` pass over the three edited modules — the first
      story in this backlog for which `check.sh` is not N/A
- [ ] `code/src/django/config/settings/CONTEXT.md`'s Critical Settings table carries the six rows
      the first scenario names, and its Module Map's `staging.py` cell names the `__Host-` divergence;
      `bash code/src/scripts/audits/docs-pairing.sh` passes over the pair
- [ ] The widened `base.py:161-162` comment names both `SESSION_COOKIE_SECURE` and
      `CSRF_COOKIE_SECURE` as intentionally absent

### Security Acceptance Criteria

<!-- Written plain at cutting; 10-security-checks numbers them from its assessment, as it did for
     US006, and may widen this list. -->

- [ ] Every module that sets a `__Host-` name satisfies the prefix's three preconditions in the
      same module — `SESSION_COOKIE_SECURE = True` and `CSRF_COOKIE_SECURE = True` present, `Path`
      at Django's default `/`, no `Domain` — proven by the gate's presence and absence clauses,
      never by a read
- [ ] A subdomain of the apex can neither read nor overwrite the apex's session or CSRF cookie:
      no `Domain` stops the read, the `__Host-` name stops the write (N-005, RFC 6265bis storage
      model), and both named limits — per cookie name; pre-prefix browsers — are stated in the
      owner rather than left to be rediscovered
- [ ] The CSRF cookie carries `HttpOnly` in every environment, no committed JavaScript reads it,
      and the token reaches HTMX only through the template — `hx-headers` or `{% csrf_token %}`
- [ ] The rename is named as a session-invalidation event at the deploy that ships it, in the
      advisory and in `ADR-US008-FIRST-MINOR-MIGRATION-KEY-09-09-2026.md`'s Context; this template
      repository deploys nothing, so no session is lost
      here, and a generated project reads the warning before it takes the update
- [ ] `CORS_ALLOWED_ORIGINS` "must be an explicit allowlist — never `*` in production" survives
      the `URL-STRATEGY.md` rewrite verbatim (`.claude/CLAUDE.md` Section 6)
- [ ] No cookie setting is loosened for dev or the test runner to make anything pass — `dev.py`
      and `test.py` unchanged; dev keeps plain names because a `__Host-` cookie cannot be insecure,
      not as a convenience
- [ ] The three clauses carry no silencing annotation, on `audits/CONTEXT.md:170`'s standing rule
      that a comment suppressing a negative-space finding is itself a finding
- [ ] The advisory prints file paths and setting names and nothing else from the project it
      inspects — never a value, on the US006 7.11 shape

### QA Acceptance Criteria — Automated

<!-- Closed by `project-management/src/18-TESTS/US008-TEST-STATUS.md`. -->

- [ ] `bash code/src/scripts/audits/negative-space.sh --self-test` exits 0; `EXPECTED` holds
      fourteen names; `broken/settings/` trips all three new clauses and `clean/settings/` trips
      none; a removed fixture still exits 2
- [ ] The discriminating comparison is recorded: the new script over the HEAD `ff24084` settings
      modules reports all three clauses; over the post-change modules it reports none — and the
      story states that the reverse comparison does not exist, so it is never claimed
- [ ] `bash code/src/scripts/audits/negative-space.sh` (unscoped — `--path` is refused) exits 0 on
      this tree after the settings land, with none of the three clauses in its skip notes
- [ ] `bash code/src/scripts/audits/doc-references.sh --path code/docs` exits 0 — "Clean — every
      citation resolves." — after the five guide edits (Q6)
- [ ] `bash code/src/scripts/audits/docs-length.sh` — the five guides (156, 185, 188, 210 and 194
      lines on 09/09/2026, all under the 270 ratchet as `wc -l` counts them, and lower as the gate
      counts) stay clear of 270 after their edits; `code/src/scripts/audits/CONTEXT.md` unchanged
      at 298 as the gate measures it. `how-to/src/TEMPLATE-GUIDE/06-GENERATION.md` is **not
      bound** and is never read by the gate — `is_instructional()` returns 0 only for a CONTEXT.md
      or CLAUDE.md basename or a path carrying a docs, workflows or .claude segment, and that path
      has none — so no length figure for it is claimed as the gate's (`code/docs/GATE-REPORTING.md`)
- [ ] `bash code/src/scripts/syntax/lint.sh` passes — the Python leg via ruff over the three
      modules and the Markdown leg via markdownlint-cli2 over the documents this story edits
- [ ] `bash code/src/scripts/syntax/check.sh` passes — it type-checks Python, which this story
      ships
- [ ] `bash code/src/scripts/tests/all.sh` is green
- [ ] ShellCheck clean over `negative-space.sh` and the advisory — **this story's own expectation,
      with no project script to satisfy it**: `lint.sh`'s legs are ruff, markdownlint-cli2, ESLint
      and clippy, and as of 08/09/2026 no script under `code/src/scripts/`, no CI job and no
      lefthook entry runs ShellCheck. Run by hand and recorded in
      `project-management/src/18-TESTS/US008-TEST-STATUS.md` as run or as not run, never as a
      `lint.sh` pass (`code/docs/GATE-REPORTING.md`)

### QA Acceptance Criteria — Manual

<!-- Closed by `project-management/src/18-TESTS/US008-MANUAL-TESTING.md`. -->

- [ ] The whole-tree `doc-references.sh` baseline is captured **before the first edit**, by
      identity — the `(file, kind, token)` triple with the line number dropped and multiplicity
      kept, the form US007's Scenario 13 fixed — with the detector's `git hash-object` recorded
      beside it and re-asserted at close; every `>` line at close is classified by whether its
      file is in this story's edit set; a count is never the test
- [ ] The five-document read-across is recorded: each of the five states the rule or defers to
      the owner, the CORS sentence is confirmed verbatim, the seven cookie-mentioning files are
      re-read and recorded harmless, and `wagtail/ADMIN-AND-PERMISSIONS.md:47-50` is recorded as
      noted and left
- [ ] The advisory dry run: against a scratch copy of this tree with `SESSION_COOKIE_DOMAIN`
      planted in `staging.py`, the script names that line with its file and line number, prints
      its two operator notes, and exits 0; against the clean tree it prints nothing and exits 0;
      with the settings directory removed it exits 0 early and prints nothing
- [ ] The preview blindness is reproduced and recorded as the Q11 row's evidence: the advisory's
      report lands in `$UPDATE_LOG` and `template-update.sh` shows none of it on a successful
      preview
- [ ] The dev-stack cookie walk: `bash code/src/scripts/development/server.sh up`, log in at
      `/control/`, submit a form, trigger an HTMX write; the browser shows plain names in dev and
      `HttpOnly` on the CSRF cookie; no console error
- [ ] A tester other than the author has signed the walk-through off

---

## Tasks

All tasks below map directly to an acceptance criterion above. Mark each complete before raising
a PR.

### Backend Tasks

- [ ] Add `CSRF_COOKIE_HTTPONLY = True` to `code/src/django/config/settings/base.py` beside
      :159-160, and widen the :161-162 comment to name `CSRF_COOKIE_SECURE`
- [ ] Add `SESSION_COOKIE_NAME = "__Host-sessionid"` and `CSRF_COOKIE_NAME = "__Host-csrftoken"`
      to `staging.py` and to `production.py`, beside :21-22 in each
- [ ] Confirm no module assigns a `Domain` or a non-`/` `Path`, and that `dev.py` and `test.py`
      are untouched
- [ ] Add the six rows to `code/src/django/config/settings/CONTEXT.md`'s Critical Settings table
      and name the divergence in its Module Map
- [ ] Run `bash code/src/scripts/tests/all.sh`, `bash code/src/scripts/syntax/lint.sh` and
      `bash code/src/scripts/syntax/check.sh`

### Script Tasks

| Task                                                                                                                                                    | Done |
| ------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| Record the discriminating comparison **before** editing any settings module: the new clauses over the HEAD `ff24084` modules                            | [ ]  |
| Add the settings-directory scope beside `SETTINGS_FILE` at `negative-space.sh:92`, and repoint it in `point_scopes_at` at `:481` **in the same change** | [ ]  |
| Add `csrf-cookie-httponly-absent` — `base.py`, presence, assignment-anchored                                                                            | [ ]  |
| Add `cookie-host-prefix-absent` — `staging.py` and `production.py`, presence of both `__Host-` names and both `SECURE = True` lines in the same module  | [ ]  |
| Add `cookie-scope-widened` — every module in the directory, absence of `*_COOKIE_DOMAIN` and of any `*_COOKIE_PATH` other than `/`                      | [ ]  |
| Give each clause the `:407` skip idiom for an absent module                                                                                             | [ ]  |
| Grow `EXPECTED` at `:506-508` to fourteen names                                                                                                         | [ ]  |
| Add `fixtures/negative-space/broken/settings/` tripping all three, and `clean/settings/` tripping none                                                  | [ ]  |
| Write `.copier/migrations/v7.6.0-host-only-cookies.sh` on the `v3.0.0` convention — advisory, `exit 0` always, proof step `negative-space.sh`           | [ ]  |
| Add the `v7.6.0` entry to `copier.yml` `_migrations:` after `v6.0.0`, before the `:921-928` comment, with the first-minor-key comment                   | [ ]  |
| Confirm `code/src/scripts/audits/CONTEXT.md` is **not** touched (Q3), and record the `:170` under-count as the named cost                               | [ ]  |

### Documentation Tasks

| Task                                                                                                                                                                                                                                                             | Done |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| Respecify `code/docs/security/CRYPTO-AND-DATA.md:122-123` — host-only, `__Host-`, the three preconditions, the two N-005 limits, the `[gate: fail]` marker inline                                                                                                | [ ]  |
| Rewrite `code/docs/URL-STRATEGY.md:149-153` to defer to the owner; keep the two surviving sentences and the `CORS_ALLOWED_ORIGINS` sentence verbatim; leave :147-148                                                                                             | [ ]  |
| Replace `(SESSION_COOKIE_PATH=/)` in `code/docs/api-design/AUTH-STRATEGY.md:67` with a deferral; let Prettier repad the table                                                                                                                                    | [ ]  |
| Add `CSRF_COOKIE_HTTPONLY = True` to `code/docs/security/AUTH-AND-AUTHZ.md` beside :138 and to `code/docs/security/OWASP-AND-CHECKLIST.md` beside :49, plus one routing sentence beneath each block                                                              | [ ]  |
| Add the `v7.6.0` row to `how-to/src/TEMPLATE-GUIDE/06-GENERATION.md` at :209's position, before `_(every)_`                                                                                                                                                      | [ ]  |
| ~~Write US008 into `project-management/src/01-FEATURE-MAPS/MAP-SUBDOMAIN-ROUTING.md:323`'s Story column~~ — **landed in the same change as this story**, the one map edit; struck through, not deleted, so the next reader sees it was done                      | [x]  |
| File the six-target map re-cut (:116, :118, :119, :120, :211, :447) as a separate `01-feature-map` correction, with the reason from the FLAGS provenance                                                                                                         | [ ]  |
| ~~Open the two `GAPS.md` rows in the :228-240 shape — the duplicated guide block (Q9); the preview blindness (Q11) — and correct the stale "22 findings" at :454 in place~~ — **all three landed in the same change as this story**; struck through, not deleted | [x]  |
| Note `code/docs/wagtail/ADMIN-AND-PERMISSIONS.md:47-50` in the manual-testing record; edit nothing there                                                                                                                                                         | [ ]  |
| At release: `VERSION`, `CHANGELOG.md`, `VERSION-HISTORY.md`, `RELEASES.md` to 7.6.0 through the `version` skill, MINOR                                                                                                                                           | [ ]  |

### QA Tasks — Automated

- [ ] Add the `broken/settings/` and `clean/settings/` fixture trees and the three `EXPECTED`
      names; run `--self-test`
- [ ] Record the discriminating comparison — three findings over the HEAD modules, none after
- [ ] Run the unscoped audit on the finished tree and confirm the three clauses ran rather than
      skipped
- [ ] Run `doc-references.sh --path code/docs` and record the exit code and verdict line
- [ ] Run `docs-length.sh`, `docs-pairing.sh`, `lint.sh`, `check.sh` and `tests/all.sh`; record
      each

### QA Tasks — Manual

- [ ] Capture the whole-tree `doc-references.sh` baseline by identity, with the detector hash,
      before the first edit; diff at close and classify every new line by the edit set
- [ ] Walk the five documents and the seven bystanders; record the read-across
- [ ] Dry-run the advisory against a planted scratch copy and against the clean tree
- [ ] Reproduce the `template-update.sh` preview blindness and record it
- [ ] Walk the dev stack: login, form POST, HTMX write, cookie flags observed

---

## Verification Checks

Run before raising a PR — via the project scripts under `code/src/scripts/**/*.sh`, never a raw
`python`, `manage.py`, `pytest`, or `docker` call.

<!-- Every template row is kept. The rows that do not apply are marked N/A with a reason rather
     than deleted, because per code/docs/GATE-REPORTING.md a skip is never reported as a pass —
     and this story is the first in the backlog where the Python rows are NOT N/A. -->

- [ ] `bash code/src/scripts/database/migrate.sh check` — **N/A**, this story touches no model and
      writes no migration
- [ ] `bash code/src/scripts/tests/all.sh --coverage` — passes, **read as a regression check**:
      the test settings module imports and every existing test is green. No floor claim is made
      on the three edited modules — they are configuration with no branch to cover, and the
      story's proof of them is the gate clause; the run is reported as what it measured
- [ ] Template, django-component, and HTMX-partial tests — **N/A**, this story adds and edits no
      template, component or partial; the HTMX CSRF path is walked by hand instead
- [ ] `bash code/src/scripts/syntax/lint.sh` and `bash code/src/scripts/syntax/check.sh` pass —
      **both apply**: ruff and markdownlint-cli2 over the Python and Markdown this story edits,
      and `check.sh`'s Python type-check over the three modules
- [ ] `bash code/src/scripts/audits/negative-space.sh --self-test` exits 0, then the unscoped run
      exits 0 with the three clauses run, not skipped — `--path` is refused by design, so there is
      no scoped form to reach for
- [ ] `bash code/src/scripts/audits/doc-references.sh --path code/docs` exits 0 at close, and
      prints "Clean — every citation resolves." — **the criterion** (Q6,
      `project-management/src/15-DECISIONS/ADR-US008-SCOPED-CITATION-BASELINE-09-09-2026.md`),
      quoted rather than summarised; on 09/09/2026 it read 174 files, 4 exempt, and tested 1353
      paths
- [ ] `bash code/src/scripts/audits/doc-references.sh --path <file>` exits 0 at close for each of
      the three shipped, scanned files this story edits outside that scope —
      `code/src/scripts/audits/negative-space.sh`, `code/src/django/config/settings/CONTEXT.md`,
      `how-to/src/TEMPLATE-GUIDE/06-GENERATION.md` — each Clean on 09/09/2026 (36, 61 and 190
      tokens checked; 0, 2 and 10 tested as paths), so a plain pass is available and honest
- [ ] `bash code/src/scripts/audits/doc-references.sh` (whole tree) — **executed before the first
      edit and at close, recorded, and never reported as passing while the baseline stands**
      (`project-management/src/15-DECISIONS/ADR-US003-CITATION-GATE-BASELINE-DIFF-02-09-2026.md`,
      :60-66 and :90-104). The regime is not contingent for this story the way it was for US006:
      US004 has not landed, so the baseline-diff branch applies, full stop. **Baseline measured
      09/09/2026 at HEAD `ff24084` on a clean working tree, detector `git hash-object`
      `b9e4e129f2781b37af3879db54af15999daa151d`: exit 1, 253 tree-wide — 135 instance, 106
      dangling, 12 template-only, no plan-prefix finding** — counted by finding line, with the
      index state and the detector recorded beside the figure because they have to be. At close
      the figure is taken again under a re-asserted hash and its delta is attributed by citer and
      by class. **Measured 09/09/2026 with this whole change staged — `git add -N` over all nine
      paths, the state it ships in: exit 1, 259 tree-wide — 140 instance, 107 dangling, 12
      template-only, no plan-prefix finding. The delta is +6, and all six are SPRINT-05.md's** —
      five instance on its bare backticked sprint tokens and one dangling on a forward reference
      to a guide family US001 creates, the two classes SPRINT-04's own cascade table carries; the
      instance class is disposed of under
      `project-management/src/15-DECISIONS/ADR-US004-INSTANCE-ARTEFACT-CITER-TEST-02-09-2026.md`
      and the inherited-red position as a whole under ADR-US003 above. This story's own artefacts
      contribute zero: the two `GAPS.md` rows carry the template-only marker and add nothing (nine
      findings before and after, the same nine); this file and both ADR-US008 records read Clean,
      0 findings each, under `--path` once in the index (the comment beneath this list has the
      untracked figures); the one map edit moves nothing. Anything else that appears at close is
      named in `project-management/src/18-TESTS/US008-MANUAL-TESTING.md` with its owner. A
      differing hash is reported as detector-confounded, never as this story's
- [ ] `bash code/src/scripts/audits/docs-length.sh` passes — exit 0 on 09/09/2026 over 777
      instructional files; the five guides and `NEGATIVE-SPACE.md` (252 counted, untouched) stay
      clear of 270; `code/src/scripts/audits/CONTEXT.md` unchanged at 298 as the gate measures it.
      `06-GENERATION.md` is outside the gate's scope (the docs-length bullet under _QA Acceptance
      Criteria — Automated_ says why) and is not reported here as measured
- [ ] `bash code/src/scripts/audits/docs-pairing.sh` passes — exit 0 on 09/09/2026, 48 `code/src`
      directories walked for Check 10. The two directories this story creates are the
      `fixtures/negative-space/broken/settings/` and `clean/settings/` trees, and the gate exempts
      the fixtures tree from Check 10 by rule — `is_exempt_from_enumeration()` in
      `code/src/scripts/audits/docs-pairing.sh:227-237`, the reason at :218-226: "a pair added
      inside one becomes live input to the audit reading it" — so neither takes a pair, and the
      pass is the exemption doing its job rather than an absence of new directories. Outside the
      fixtures it edits one half of the `config/settings/` pair and the pair stays whole
- [ ] `bash code/src/scripts/audits/doctrine-drift.sh` passes — regression only: it reads fenced
      code, and no claim row in its table covers a settings line, so it cannot see the two blocks
      this story edits; never reported as though it had looked
- [ ] ShellCheck over `negative-space.sh` and the advisory — the story's own expectation, no
      project script satisfies it; recorded as run or as not run, never as a `lint.sh` pass
- [ ] No secrets, debug flags, or hardcoded IDs introduced — the two `__Host-` names are literals
      by design and not secrets; `DEBUG` is untouched in every module
- [ ] GDPR section reviewed — **N/A**, the flag reads `N/A`; no personal-data path changes
- [ ] Logging plan satisfied — **N/A**, the flag reads `N/A`; nothing here logs
- [ ] Security acceptance criteria signed off — applies; the flag is set
- [ ] SEO acceptance criteria signed off — **N/A**, the flag reads `N/A`; no public page changes
- [ ] Accessibility (WCAG 2.2 AA) — **N/A**, this story renders no interactive surface

<!-- This file's own citation-gate findings, measured 09/09/2026 with
     bash code/src/scripts/audits/doc-references.sh --path project-management/src/02-STORIES/US008.md
     immediately after it was written, under detector b9e4e129f2781b37af3879db54af15999daa151d:
     19, of which 12 are [template-only citation] against full-path PM src/ citations — the
     git-index artefact US004 repairs, since build_template_only() reads git ls-files and an
     UNTRACKED story is treated as shipping; every one of the 12 vanishes once the file is added
     to the index, exactly as US004's own A/B records — and 7 are [instance citation] on bare
     backticked sprint, plan and story tokens. The 7 were removed by spelling: full paths where a
     path exists, plain text where the artefact did not then exist (SPRINT-05.md and its plan;
     the record exists from this change, the plan still does not, and both stay plain here), and the
     story's own number unbackticked. Re-measured after that pass under the same detector: 16,
     every one [template-only citation] and every one the git-index artefact, zero [instance
     citation]. So once this file is tracked its own contribution to the whole-tree delta is
     expected to be zero, and the manual-testing record confirms or corrects that beside the
     baseline; it is a report, not a criterion. Paths in this comment are unbackticked because
     the gate reads backticked tokens inside comments too.
     Re-measured 09/09/2026 after the correction pass that fixed the class split and completed
     the refusal enumeration above, with the file in the index (git add -N) and under the same
     detector: Clean, 0 findings — the git-index artefact confirmed, and this file's contribution
     to the whole-tree figure is zero. The 19 and 16 above describe the file as first written,
     untracked.
     Re-measured 09/09/2026 a third time, after the SPRINT-05 pass corrected the sprint-membership
     bullet, the not-in-scope bullet and the Definition of Done row above for the record that now
     exists, under the same detector: Clean, 0 findings, exit 0 — 344 backticked tokens checked, 42 tested as
     repo paths, one more than before (project-management/docs/planning/SPRINTS.md, which
     resolves). SPRINT-05.md is still named in plain text here, as it was.
     Re-measured 09/09/2026 a fourth time, after the pass that withdrew the docs-length claim over
     how-to/src/TEMPLATE-GUIDE/06-GENERATION.md (the gate never reads it) and reconciled the
     whole-tree figures to the index state each was taken under — 253 at HEAD ff24084 on a clean
     tree; 259 with the whole change staged, the +6 SPRINT-05.md's alone and none of it this
     story's — under the same detector: Clean, 0 findings, exit 0 — 358 backticked tokens
     checked, 44 tested as repo paths. SPRINT-05.md stays in plain text here. -->

---

## Definition of Done

- [ ] All acceptance criteria met and verified by a reviewer
- [ ] All tasks checked off
- [ ] All verification checks passed
- [ ] Both `ADR-US008-*` records — written 09/09/2026 in the same change — committed with this
      story and cited above by full repo-relative path;
      `ADR-US003-CITATION-GATE-BASELINE-DIFF-02-09-2026.md` cited and **unchanged** — both its
      supersession fields still read `—`
- [ ] No `[OPEN]` acceptance-criteria gap remains in this story's QA plan under
      `project-management/src/11-QA/PLANNING/`
- [ ] Code reviewed and approved (minimum 1 reviewer)
- [ ] No outstanding TODO or FIXME comments introduced by this story
- [ ] `project-management/src/01-FEATURE-MAPS/MAP-SUBDOMAIN-ROUTING.md:323` carries US008 in its
      Story column, and lines :116, :118, :119, :120, :211 and :447 are byte-identical to
      `82ec176` — the re-cut is filed separately, not smuggled
- [ ] SPRINT-05 was opened by `03-sprint-planning` in the same change as this story, with this
      story as its sole member and no sprint plan, and the ten refusal statements of 07/09/2026 —
      enumerated in full under Dependencies, every line number as at HEAD `ff24084` — are
      disposed of there: the three that go false or stale once the record exists, the bullet
      under _Won't (this sprint)_ in `project-management/src/16-SPRINT-PLANS/04-SPRINT-PLAN-04.md`
      first among them, superseded in dated comments; the seven that stay true untouched; none
      left silently contradicted
- [ ] `GAPS.md` carries the two rows and the corrected :454 figure — written 09/09/2026 in this
      change rather than at `22-implementation-documentation`, which is recorded here as a
      departure from that workflow's ownership of `GAPS.md` writes, not hidden; the rows are
      closed by whoever takes them, never by this story
- [ ] At release, `VERSION` reads `7.6.0`, the four version files agree, the tag exists, and the
      `copier.yml` migration key matches it
- [ ] Merged to `main` (or the active release branch)
- [ ] Story status updated to **Completed** in the project board
- [ ] Any GDPR or security gaps identified during review are documented in the sprint note
