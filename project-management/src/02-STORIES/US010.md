# US010 — The seven register indexes are born seeded, and the map index leaves the file that ships

**Epic:** Register Indexes
**Status:** Open

<!-- FLAGS — one row per gate, and the flag is that gate's entry condition.
     A flag reading N/A means the gate is skipped for this story; every other value means it runs.
     The value is a MANIFEST, not the design: the gate owns the design, may add to this list, and
     the story is updated to match when the gate closes.

     PROVENANCE. Cut from project-management/src/01-FEATURE-MAPS/MAP-REGISTER-INDEXES.md slices
     `S-01` and the narrowed `S-02`, nodes `N-001` ✅, `N-002` ✅, `N-005` ✅ and `N-006` ✅, all
     settled 31/08/2026. That map's frontier is empty and its `Gate to stories` records a deadlock
     ruling that names this slice by number: "`S-01` may be cut on that basis; the box closes when
     `S-01` ships".

     BOTH SLICE ROWS CARRY `TBD` IN THEIR `Nodes` AND `Acceptance` CELLS, and both are back-filled
     from the map's own Batch A and Batch B sections in the same change as this story. That is an
     edit beyond `02-story-creation` STEPS.md Step 4's stated scope and is recorded here rather
     than made silently — the precedent is US004.md:12-16, where `MAP-RULE-OWNERSHIP.md` `S-06`
     had the same two cells empty at cutting.

     `S-02` IS SPLIT AT THIS GATE, 20/09/2026, ON THE US005 PRECEDENT. `S-02` as charted read
     "Backfill `MAP-INDEX.md`, and correct the rows it indexes" against a population the map
     records as "the ten maps". The population was re-measured at this gate and is **14 maps** —
     15 within the hour, see below;
     worse, the same measurement found the real backfill obligation is not ten rows but **56**,
     across all seven registers — `01-FEATURE-MAPS` 14, `02-STORIES` 9, `03-SPRINTS` 5,
     `15-DECISIONS` 20, `17-STORY-PLANS` 8, `20-FINDINGS` 0, `21-BUGS` 0. Fifty-six hand-written
     one-line summaries on top of this story's mechanism prices at 13 or above, which
     project-management/docs/planning/STORIES.md calls an epic by SHAPE, not by size.

     The split is made at the only seam that is real. This story builds the MECHANISM once — seven
     files, seven seeds, seven `_tasks` lines, seven `SEEDED` registrations, seven `CONTEXT.md`
     sections — and backfills `MAP-INDEX.md`, which is the row the five parked maps are actually
     waiting on. `US011` fills the other four. `S-02` narrows to `MAP-INDEX.md` and a NEW `S-05`
     row carries the four-register backfill, appended rather than suffixed, exactly as
     `MAP-RETRY-AND-IDEMPOTENCY.md` appended `S-09` when `S-02` split at this same gate on
     04/09/2026 (US005.md:12-20).

     THE MAP COUNT MOVED UNDER A CONCURRENT SESSION WHILE THIS STORY WAS BEING WRITTEN, AND THE
     STORY IS WRITTEN NOT TO CARE. `01-FEATURE-MAPS/` held **14** maps when the slice was selected
     on 20/09/2026 and **15** an hour later: `MAP-NATIVE-MOBILE-SURFACE.md` — "Retire React
     Native; the mobile surface becomes Kotlin and Swift", `Status: Charting`, 21 open / 9
     blocking, four slices — was created by a parallel session at 10:29 and is untracked at the
     time of writing. Nothing about this story changes, because every criterion below asserts ONE
     ROW PER MAP COUNTED AT IMPLEMENTATION TIME and none holds a literal. The figure of 14 is kept
     in this block as the cut-time measurement with its date, which is what makes the drift
     visible rather than silently absorbed.

     The precedent is `MAP-RULE-OWNERSHIP.md` Batch A, which found `N-016` had "shrunk under a
     parallel session and grown back by one" between charting and settlement — the reason that map
     refused to cut a story until its frontier was empty. A story that hardcodes a population it
     measured once is a story that is wrong by the time it is built.

     SIX OF THE MAP'S OWN FIGURES ARE STALE, RE-MEASURED 20/09/2026 AT THIS GATE:
       - "the ten maps" (N-006) → **14**.
       - `copier.yml:908-919` for the `_tasks` mv block → the block is at **`:973-984`**, and it
         ends `rmdir .copier`, so seven new `mv` lines land ahead of that line.
       - "`SPRINT-00-TEMPLATE.md` has no `**Status:**` field", declared to ride `S-01` as "a
         one-line repair `SPRINT-INDEX.md` depends on" → **already fixed**. That file reads
         `**Status:** Planned` at `:10`. The rider is DISCHARGED, not carried.
       - `MAP-000-TEMPLATE.md:146` for the template's own index-row checklist line → that line
         is at **`:159`**, and `doc-references.sh:322` for the `SEEDED` parse → **`:373`**, where
         it reads the `mv .copier/...` lines out of `_tasks` rather than a named array. GAPS.md's
         own citation of `01-FEATURE-MAPS/CLAUDE.md:22-23`/`:26` is off by one in the same
         direction — the instruction is at **`:21-22`** and the definition-of-done at **`:25`**.
         Line drift in every case; substance intact.
       - "The Plans Index is gated in eight places and has never existed" → the file still does not
         exist, but the eight sites are not eight. A pass on 08/09/2026 rewrote the four shipped
         plan-template sites in place, and `US007.md:170` already binds this slice: "`S-01`
         re-measures its population from the tree as it stands, rather than inheriting the map's
         count of eight". This story re-measures.

     ONE OF THE MAP'S CLAIMS IS STALE IN THE OTHER DIRECTION. Batch B recorded
     `17-STORY-PLANS/CONTEXT.md` as having "no such H2". It has one — `## The plans index` at
     `:81-91`, written by the 08/09/2026 pass, which explains the absence and names "the
     register-index work charted in `../01-FEATURE-MAPS/`" as what creates the file. That section
     is REWRITTEN by this story, not added.

     WIDENED BEYOND THE SLICE, settled at `02-story-creation` on 20/09/2026 (round 2 Q2). `S-01`
     scopes itself to `MAP-000-TEMPLATE.md:8`/`:159` plus the seven `CONTEXT.md` sections. GAPS.md's
     entry of 01/09/2026 (re-measured 17/09/2026) names four further sites that instruct the old
     row into a shipped file and sit in no slice on any map — `.claude/skills/wayfinder/SKILL.md:97-98`
     and `:256`, and `project-management/src/01-FEATURE-MAPS/CLAUDE.md:21-22` and `:25`. That entry's
     own first suggested action is to "widen `MAP-REGISTER-INDEXES.md` S-01's Acceptance to name the
     three sites, so they are repaired in the same change as the seeded map index ships". It also
     warns that the decline runs under THREE different rationales and "whoever repairs this must
     dispose of all three, not just the one with an owner" — which is why the 14-map checklist
     sweep is inside this story rather than left as prose maintenance.

     THE ENUM SEPARATOR IS ` · `, NOT ` — `, AND THE CHANGE IS MEASURED. Round 4 Q1 settled that a
     map's header becomes `<enum> — <existing prose>` so the index can mirror a value that
     string-equals. The four-value enum at `MAP-000-TEMPLATE.md:4` is
     `Charting / Resolving / Blockers clear — stories may start / Complete`, and the third value
     CONTAINS an em-dash — so "the text before the em-dash" is ambiguous for one value in four,
     and a gate built on it would mis-read exactly the status this map itself carries. The
     separator is therefore the middle dot the repository already uses for header fields
     (`**Frontier open**: 0 · **Blocking open**: 0`). The decision is unchanged; its punctuation is
     corrected before it is written down.

     SECURITY DIVERGES FROM THE SLICE MANIFEST, on the US004 Q6 and US006 precedents. `S-01`'s
     manifest names QA only. This story edits `copier.yml`'s `_tasks` list, whose `copy` gate is
     the seed-once mechanism: if a `mv` lands outside `when:` gate keyed on `_copier_operation == 'copy'`,
     a `copier update` hands a project its blank index stub back OVER a filled-in index. That is
     the one-way door `copier.yml:967-972` names in its own words for `MEMORY.md`, `GAPS.md` and
     `DEFERRED.md`, and it is gated there by `.github/scripts/shipped-memory.sh` and
     `shipped-registers.sh` rather than trusted. US006.md:15-22 settled the rule: "a manifest
     written for one kind of slice must not skip the only gate able to check what the story
     actually ships. QA proves the states it was told to prove; Security asks whether the state
     list is right." The other eleven rows are written `N/A` rather than left blank —
     STEPS.md:62-70, "a row left blank silently skips a gate".

     ONE THING THIS GATE DID NOT SETTLE, recorded rather than hidden: the gate that reads these
     indexes is `N-003` and lives in `S-03`, which is a LATER story. This story ships seven index
     files and four of them are deliberately incomplete. Nothing mechanical stops that state
     persisting — only `US011`, which is cut in the same session and named inside each incomplete
     file. If `15-decisions` or the story plan wants the gate pulled forward to close that window,
     this is the criterion to change. -->

| Flag       | Value                                                                                                                                                                                                                                                                                       |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DB         | N/A                                                                                                                                                                                                                                                                                         |
| User Flow  | N/A                                                                                                                                                                                                                                                                                         |
| Brand      | N/A                                                                                                                                                                                                                                                                                         |
| Components | N/A                                                                                                                                                                                                                                                                                         |
| Wireframes | N/A                                                                                                                                                                                                                                                                                         |
| GDPR       | N/A                                                                                                                                                                                                                                                                                         |
| Security   | seed-once integrity — all seven `_tasks` `mv` lines inside the existing `copy` gate, so no `copier update` can overwrite a project's filled index with a blank stub; no seed cut from a populated in-tree index                                                                             |
| QA         | unit, integration, manual — `shipped-artefacts.sh --self-test` (seed-lands, seed-deleted); generation smoke on both `INCLUDE_MOBILE` answer sets; update-does-not-overwrite; row-per-map and counts-match-header; the four instruction sites and the two template sites re-read as repaired |
| SEO        | N/A                                                                                                                                                                                                                                                                                         |
| API        | N/A                                                                                                                                                                                                                                                                                         |
| Logging    | N/A                                                                                                                                                                                                                                                                                         |
| Backend    | N/A                                                                                                                                                                                                                                                                                         |
| Frontend   | N/A                                                                                                                                                                                                                                                                                         |

<!-- THE COPIER DELIMITERS ARE NOT REPRODUCED ANYWHERE IN THIS FILE, and the omission is
     deliberate. `.github/scripts/check-template-tokens.sh` scans every non-exempt tracked file for
     the copier delimiter pair and fails anything between them that is not a bare upper-snake token
     name, which a quoted copier conditional is not. Its exemption list covers `project-management/src/01-FEATURE-MAPS/` but not
     `02-STORIES/` or `03-SPRINTS/`, and widening it would be wrong rather than merely bold: the
     `_exclude` re-includes mean `**/CONTEXT.md`, `**/CLAUDE.md` and `**/*TEMPLATE*` under that tree
     DO render, so a blanket folder exemption would stop gating files that genuinely ship. The gate
     has no way to say "this is a quoted example in a file that cannot render", so the `when:` key
     and its condition are named without the delimiters around them — and THIS COMMENT does not
     reproduce them either, which is why it describes the pair rather than showing it. The same
     technique US011 used for a literal HTML comment terminator on the same day. Measured 20/09/2026, when the
     pre-commit hook blocked on four sites across this file and the sprint record. -->

---

## Client Summary

Each folder that collects project records — feature maps, stories, sprints, decisions, plans,
findings and bugs — gains a contents page listing what is inside it, and a new project starts with
those contents pages already in place and empty. Today the feature-map folder's contents page says
"none charted yet" while fourteen maps sit beside it, and the instruction that produced that
mistake is repeated in four other files. This change puts each list in its own file, fills the
feature-map one in, and removes the instruction that made it wrong.

---

## User Story

As a **developer or agent returning to a register folder I did not fill**, I want **an index file
inside it that lists every instance, and a shipped instruction that points at that file rather
than at one which cannot hold the rows**, so that **I can see what a folder contains without
reading every file in it, and a generated project starts from an index that is empty and correct
rather than one that is populated and false**.

## MoSCoW Priority

**Must Have** <!-- Must Have / Should Have / Could Have / Won't Have -->

<!-- `Must`, and the warrant is a queue rather than an urgency. FIVE maps have parked their
     unticked `Gate to stories` index-row box on this slice shipping, and
     `MAP-REGISTER-INDEXES.md`'s own gate calls the resulting state "a deadlock" —
     "The gate binds the work that repairs the gate". `MAP-NAVIGATION.md` has already downgraded
     its own refusal from a decision to a defect in writing: "Index row still not added — the
     shipped `CONTEXT.md` index reads 'None charted yet' and this is now a defect rather than a
     decision."

     The precedent for this shape is US002, made wave-0 `Must` on the ground that eight slices
     across seven maps blocked on audit-register headroom. This blocks more: six maps plus a live
     GAPS.md entry plus `US011`, which cannot start until the files exist. -->

## Story Points

<!-- Rough guide: 1–2 = trivial · 3–5 = moderate · 8 = large · 13 = extra-large (split candidate) -->

8

<!-- 8, and the split that keeps it there is recorded in the Provenance block above. The combined
     `S-01` + unsplit `S-02` measured at 13 or above once the backfill population was counted at 56
     rows rather than the map's ten; `project-management/docs/planning/STORIES.md` puts the epic
     threshold at 13 and is explicit that it "is not a size judgement — it is a shape one".

     8, not 5, against US004's 8 for one script plus its five repairs and its fixtures. The surface
     here is wider — seven files, seven seeds, seven `_tasks` lines, seven registrations, seven
     `CONTEXT.md` sections, a status header and a checklist box per map, six instruction sites —
     but each is
     shallow, and indexes two through seven are the same edit as index one. What earns the 8 rather
     than the 5 is that all seven TAILS and ORDERING RULES are designed here (round 4 Q2): seven
     registers run seven status vocabularies carried four different ways, and a file cannot be
     created without its columns.

     If `15-decisions` surfaces a second `copier.yml` decision — anything touching the `_tasks`
     ordering or the `copy` gate beyond a straight seven-line addition — this goes to 13 and back
     to `01-feature-map` to be re-cut. -->

## Dependencies

- **None blocking.** Every node this story carries is resolved, and nothing it touches is created
  by an unshipped story. Verified 20/09/2026 against `US001`–`US009`, all `Status: Open`: none of
  them writes `copier.yml`'s `_tasks` list, `.github/scripts/shipped-artefacts.sh`'s `SEEDED`
  array, `.claude/skills/wayfinder/SKILL.md`, or any `CONTEXT.md` under
  `project-management/src/`.
- **A concurrent session is charting `MAP-NATIVE-MOBILE-SURFACE.md`**, untracked as of
  20/09/2026. It is not a blocker — this story indexes whatever maps exist when it is built — but
  `.ai/INSTRUCTIONS.md` requires shared-artefact ownership to be coordinated, and
  `01-FEATURE-MAPS/` is shared between the two sessions. If that map lands first it gains a row
  like any other; if it lands mid-story the folder is re-counted, which is why no criterion holds
  a literal.
- **`US011` depends on this story** — it backfills four indexes this one creates. Named in both
  directions so a later reader does not re-merge them.
- **`US007` already recorded the handoff.** `US007.md:170` names this line of work as the owner of
  `STORY-PLAN-INDEX.md` and the sites that once gated on it, and states that this slice
  "re-measures its population from the tree as it stands, rather than inheriting the map's count of
  eight". That re-measure is a task below, not an assumption.
- **`MAP-REGISTER-INDEXES.md` `S-03` — the gate — ships after this story, not before.** `N-003`
  asserts presence, symmetry and status over these files; it cannot run until they exist. Named so
  the absence of a gate in this story is read as sequencing rather than omission.
- **Sprint membership: this story is the sole member of `SPRINT-06`.** At 8 SP `Must` it sits
  inside the 11 SP capacity with no stretch tier, and `US011` at 8 SP `Should` cannot join it —
  16 SP exceeds both capacity and the 13 SP grace ceiling. `SPRINT-01` to `SPRINT-05` are all
  closed. The sprint record itself is written by `03-sprint-planning`, not here.

## Decisions

<!-- None yet, and the section is written rather than left blank. At cutting, no decision this
     story made was hard to reverse: the slice split follows a standing precedent, the four stale
     figures are corrections to measurement rather than choices, and the enum separator is a
     punctuation fix inside a decision already taken. Two ADR candidates are named because they are
     the ones `15-decisions` should expect to be asked, not because they have been settled. -->

- **None — no decision this story has made so far was hard to reverse.**
- **ADR candidate, for `15-decisions` to accept or decline:** the four incomplete indexes ship
  declaring their own debt rather than being withheld until `US011` fills them. This is the
  round-3 Q1 answer, and it is the one place this story knowingly puts a false-looking file in the
  tree. If it earns a record it is because the alternative — building the mechanism twice — was
  rejected on a named ground.
- **ADR candidate:** the map `Status` header gains a four-value enum prefix, making every map's
  status machine-readable for the first time. It is hard to reverse in the sense that `S-03`'s
  gate will be built against it.

---

## Acceptance Criteria

Seven register folders each carry an index file with a shared spine and a stated ordering rule.
`MAP-INDEX.md` lists all fourteen maps. The other six ship as the mechanism intends — four of them
naming the story that fills them, two legitimately empty. A generated project receives all seven
blank, seeded once, and no update can overwrite a filled one. The six shipped files that instruct
the old index row stop doing so.

```gherkin
Scenario: Every register that accumulates instances carries an index file
Given the seven registers named by Q36 — 01-FEATURE-MAPS, 02-STORIES, 03-SPRINTS, 15-DECISIONS, 17-STORY-PLANS, 20-FINDINGS, 21-BUGS
When the tree is read
Then each folder contains an index file named for the folder's own noun, singularised
And the seven names are MAP-INDEX.md, STORY-INDEX.md, SPRINT-INDEX.md, DECISION-INDEX.md, STORY-PLAN-INDEX.md, FINDING-INDEX.md and BUG-INDEX.md
And 23-INCIDENTS/INCIDENT-INDEX.md is unchanged, because the naming rule explains it rather than special-casing it

Scenario: Every index carries the shared spine and states its own tail and order
Given any one of the seven index files
When its table is read
Then it carries the spine columns Status, Instance, Summary and Updated, in that order
And any further columns are that register's own tail, and are named in the file
And the file states its own ordering rule in prose — descending by date for MAP, FINDING and BUG; ascending by identifier for STORY, SPRINT, STORY-PLAN and DECISION
And the Status column holds the artefact's own value mirrored verbatim, never translated

Scenario: MAP-INDEX.md carries a row for every map in the folder
Given an instance is a tracked MAP-*.md file that is not MAP-000-TEMPLATE.md
And the population is counted from the tree at implementation time, never from the figure of 14 this story was cut against
When MAP-INDEX.md is read
Then every map in the folder has exactly one row
And no row names a map that is absent
And each row's Instance cell links to the map and is labelled with its descriptor
And each row's Summary is one line of plain English for someone who has not read the map
And no row asserts the retired no-ADR house rule in an Umbrella ADRs cell

Scenario: Every map's Status becomes a value the index can mirror
Given MAP-000-TEMPLATE.md:4 defines exactly four status values
When every map's **Status** header is read
Then it begins with one of Charting, Resolving, Blockers clear — stories may start, or Complete
And any existing prose follows after a middle-dot separator, never an em-dash, because the third enum value contains one
And the value is derived by measurement — Complete only where both frontier and fog of war are empty — never asserted to make a map look finished
And MAP-INDEX.md's Status cell string-equals that leading enum value

Scenario: The four indexes this story does not backfill declare their own debt
Given STORY-INDEX.md, SPRINT-INDEX.md, DECISION-INDEX.md and STORY-PLAN-INDEX.md ship with no rows
And their registers hold 9, 5, 20 and 8 instances respectively
When each file is read
Then it carries a visible line naming US011 as the story that fills it
And the file does not read as an empty register

Scenario: Two indexes are legitimately empty and say nothing about a debt
Given 20-FINDINGS and 21-BUGS hold zero instances
When FINDING-INDEX.md and BUG-INDEX.md are read
Then each carries its spine, its tail and its ordering rule with an empty-register row
And neither names US011, because neither is owed anything

Scenario: A generated project receives all seven indexes, blank
Given a project generated from this template on either INCLUDE_MOBILE answer set
When project-management/src/ is read in the generated tree
Then all seven index files are present
And six of them carry no instance rows
And MAP-INDEX.md carries exactly one row, for the seeded MAP-SCALE-PLANNING.md, with every column reading TBD
And no index in the generated tree names a syntek-base map, story, sprint, decision or plan

Scenario: The seeds are seed-once, and an update can never overwrite a filled index
Given a generated project whose indexes have been filled in
When copier update is run against a later template version
Then no index file in that project is replaced by its blank seed
And every one of the seven mv lines sits inside the existing when: _copier_operation == 'copy' gate
And the seven mv lines land ahead of the rmdir .copier line in the same task

Scenario: The template-integrity gate admits the seven seeds
Given .github/scripts/shipped-artefacts.sh treats a file under project-management/src/ as a leak unless it is registered
When the script runs against a generated project
Then its SEEDED array names all seven index files alongside MAP-SCALE-PLANNING.md
And the script does not report any of them as leaked
And --self-test still fires when a seeded file is deleted

Scenario: The map index leaves the file that ships
Given project-management/src/01-FEATURE-MAPS/CONTEXT.md:43-51 carries a ## Map index table reading "None charted yet"
When the change is applied
Then that table is gone from the shipped file
And a ## The index section replaces it, linking MAP-INDEX.md and stating how to read a row
And the section follows the 23-INCIDENTS/CONTEXT.md:57-62 shape, which is the only working precedent
And the prose at :53-56 explaining why MAP-SCALE-PLANNING.md has no row is retired with the table

Scenario: Every register's CONTEXT.md routes to its own index
Given the seven registers
When each CONTEXT.md is read
Then each carries a ## The index H2 linking its index file and stating the reading rule
And 17-STORY-PLANS/CONTEXT.md:81-91 — which today explains the index's absence — is rewritten to describe its presence
And 23-INCIDENTS/CONTEXT.md is unchanged, being already correct

Scenario: No shipped file still instructs a row into a shipped file
Given the six sites measured on 20/09/2026
When each is read after the change
Then MAP-000-TEMPLATE.md:8 and :159 name the seeded index file, not CONTEXT.md
And .claude/skills/wayfinder/SKILL.md:97-98 and :256 name the seeded index file
And project-management/src/01-FEATURE-MAPS/CLAUDE.md:21-22 and :25 name the seeded index file
And a grep for the old instruction across the repository returns nothing outside dated historical comments

Scenario: All three decline rationales are disposed of, not just the one with an owner
Given the maps decline the index row under three different rationales
When each map's Gate to stories index-row box is read after the change
Then the box is ticked where the row now exists in MAP-INDEX.md
And any box left unticked names a reason that is still true after this story
And no map's decline still cites MAP-REGISTER-INDEXES N-001 as the reason, that rationale having been retired by this story
```

### Security Acceptance Criteria

<!-- `ST` numbers are allocated by `10-security-checks`; the bracket carries the story until it
     runs. The subject is the generation seam, not an endpoint — this story ships no runtime code. -->

- [ ] [US010/ST01] All seven `mv` lines sit inside the existing `_tasks` entry gated
      `when:` gate keyed on `_copier_operation == 'copy'` (`copier.yml:984`) — asserted in the diff, not
      assumed. An ungated `mv` overwrites a project's filled index with a blank stub on every
      `copier update`, which is the one-way door `copier.yml:967-972` names for `MEMORY.md`,
      `GAPS.md` and `DEFERRED.md`
- [ ] [US010/ST02] All seven `mv` lines land **before** `rmdir .copier` in the same `&&` chain
      (`copier.yml:983`). A line after it silently no-ops and the project is generated without the
      index it was promised
- [ ] [US010/ST03] No `.copier/` seed is cut from a populated in-tree index. Each seed is authored
      blank — or, for `MAP-INDEX.md`, with exactly one `TBD` row — and the emptiness is **proved by
      a check, not trusted**, on the `shipped-memory.sh` / `shipped-registers.sh` precedent that
      `copier.yml:967-972` sets for exactly this hazard
- [ ] [US010/ST04] No index seed names a syntek-base map, story, sprint, decision, plan, finding or
      bug. A seed cut from this repository's own rows is a one-way door: it ships that content
      permanently and no later update can correct it
- [ ] [US010/ST05] The change introduces no new network fetch, no new credential read, and no write
      outside `project-management/src/`, `.copier/`, `copier.yml`,
      `.github/scripts/shipped-artefacts.sh` and `.claude/skills/wayfinder/SKILL.md`

### QA Acceptance Criteria — Automated

- [ ] Unit: `bash .github/scripts/shipped-artefacts.sh --self-test` passes with the enlarged
      `SEEDED` array, and its planted-deletion probe still fires — a deleted seeded index is
      reported, not silently tolerated
- [ ] Integration: the `[3/4] Template Generation` job generates a project on **both** answer sets
      (`.github/workflows/audit-template.yml:151-161`, `INCLUDE_MOBILE` false and true) and all
      seven index files are present in each generated tree
- [ ] Integration: a grep over each generated tree finds no `US###`, `SPRINT-##`, `ADR-US###` or
      `MAP-<FEATURE>` literal inside any index file, bar the seeded `MAP-SCALE-PLANNING.md` row
- [ ] Integration: a `copier update` probe against a generated project whose indexes have been
      edited asserts that **no** index file is reverted to its seed
- [ ] `bash code/src/scripts/audits/docs-pairing.sh` passes — every register folder still carries
      its `CONTEXT.md` + `CLAUDE.md` pair, and an index file is not mistaken for one
- [ ] `bash code/src/scripts/audits/doc-references.sh` passes — every citation this story writes or
      repoints resolves, and the six repaired instruction sites raise no finding
- [ ] `bash code/src/scripts/audits/docs-length.sh` passes — the seven `CONTEXT.md` files each gain
      a section, and none crosses the 300-line ceiling or the 270 ratchet without a dated allowance

### QA Acceptance Criteria — Manual

<!-- Manual because a row's Summary is prose written for a human, and no script can judge whether
     one line of plain English describes a map to someone who has not read it. -->

- [ ] `MAP-INDEX.md` read row-by-row against the map files as the folder then stands — every row's
      `Status`, `Updated` and
      slice count matches its map's own header, and each `Summary` is intelligible without opening
      the map
- [ ] Every map's `**Status**` header read against the four-value enum, and the derivation
      recorded — which maps are `Complete`, which are `Blockers clear — stories may start`, and why
      a map with open fog is not `Complete`
- [ ] The four instruction sites and two template sites re-read after the change and confirmed to
      instruct the new file, with the re-read recorded in the implementation record
- [ ] The four incomplete indexes read as incomplete-and-owned rather than empty-and-correct, by a
      reader who has not read this story
- [ ] A tester other than the author has signed the walk-through off

---

## Tasks

All tasks below map directly to an acceptance criterion above. Mark each complete before raising a
PR.

### Index Tasks

- [ ] Design the shared spine once — `Status · Instance · Summary · Updated` — and write it into
      all seven files
- [ ] Design each register's **tail columns and ordering rule** and state the order in the file.
      Seven registers, four different status carriers, measured 20/09/2026: `02-STORIES`,
      `03-SPRINTS` and `15-DECISIONS` carry `**Status:** <value>` as a bold prose line;
      `17-STORY-PLANS` carries `| Status | \`<value>\` |`as a **backticked table cell**;
 `01-FEATURE-MAPS` carries free prose until this story's enum lands
- [ ] Give `15-DECISIONS`'s tail a supersession column pair — `Supersedes` / `Superseded by` —
      per `ADR-US000-TEMPLATE.md:13-14`; the spine's single `Status` cannot hold a chain
- [ ] Create all seven index files in-tree, following the `23-INCIDENTS/INCIDENT-INDEX.md` shape:
      title, metadata line, reading rule, `## The register` table, `## How to read a row`
- [ ] Backfill `MAP-INDEX.md` with one row per map, **re-counting the folder at implementation
      time** — the count moved from 14 to 15 under a concurrent session on the day this story was
      cut, and will move again. Measured from the map headers rather than from this
      story's own count
- [ ] Add the `> Backfill owed — US011` line to `STORY-INDEX.md`, `SPRINT-INDEX.md`,
      `DECISION-INDEX.md` and `STORY-PLAN-INDEX.md`, and **do not** add it to `FINDING-INDEX.md` or
      `BUG-INDEX.md`
- [ ] Re-measure the Plans Index citation population from the tree as it stands, per `US007.md:170`
      — the map's count of eight is not inherited

### Seed and Generation Tasks

- [ ] Author seven blank seeds under `.copier/`, each carrying its spine, tail and ordering rule
      and no instance rows
- [ ] Give `.copier/MAP-INDEX.md` its one `TBD` row for the seeded `MAP-SCALE-PLANNING.md`, so a
      generated project's index is not blank beside a file that exists
- [ ] Add seven `mv` lines to the `_tasks` entry at `copier.yml:973-984`, ahead of
      `rmdir .copier`, targeting `project-management/src/<REGISTER>/<NOUN>-INDEX.md` — the nested
      target precedent is the existing `MAP-SCALE-PLANNING.md` line
- [ ] Confirm the `when:` gate keyed on `_copier_operation == 'copy'` gate covers all seven; add no second
      task
- [ ] Grow `SEEDED` at `.github/scripts/shipped-artefacts.sh:105` from one entry to eight
- [ ] Add a `--self-test` probe that deletes a seeded index and asserts the finding fires
- [ ] Confirm no `_exclude` negation is needed — `/project-management/src/**` at `copier.yml:157`
      already covers the path, and `doc-references.sh:373` parses the `mv .copier/...` lines out of `_tasks`

### Documentation Tasks

- [ ] Replace `## Map index` at `01-FEATURE-MAPS/CONTEXT.md:43-51` with a `## The index` section
      linking `MAP-INDEX.md`, and retire the `:53-56` prose about `MAP-SCALE-PLANNING.md`'s missing
      row
- [ ] Add a `## The index` H2 to `02-STORIES`, `03-SPRINTS`, `15-DECISIONS`, `20-FINDINGS` and
      `21-BUGS` `CONTEXT.md` files
- [ ] **Rewrite** `17-STORY-PLANS/CONTEXT.md:81-91` — the section exists and explains the index's
      absence; it now describes its presence
- [ ] Leave `23-INCIDENTS/CONTEXT.md` unchanged — already correct, and the naming rule explains it
- [ ] Repoint `MAP-000-TEMPLATE.md:8` and its checklist line at `:159`
- [ ] Repoint `.claude/skills/wayfinder/SKILL.md:97-98` and `:256`
- [ ] Repoint `project-management/src/01-FEATURE-MAPS/CLAUDE.md:21-22` and `:25`
- [ ] Update each touched `CONTEXT.md`'s directory tree and `**Last Updated**` date

### Map Tasks

- [ ] Prefix **every** map's `**Status**` header with its enum value, separated from any existing
      prose by a middle dot rather than an em-dash,
      deriving the value by measurement rather than assertion
- [ ] Tick or re-justify **every** map's `Gate to stories` index-row box, disposing of **all
      three** decline rationales named in `GAPS.md` (17/09/2026), not only N-001's
- [ ] Confirm `MAP-REGISTER-INDEXES.md`'s `S-01`, `S-02` and `S-05` rows still read as
      `02-story-creation` left them on 20/09/2026 — `Story` cells filled, `Nodes` and `Acceptance`
      back-filled, `S-02` narrowed to `MAP-INDEX.md`. **Verify, do not re-cut.** Those edits are
      the gate's own and were made when this story was written; a second pass here would be a
      story editing a wayfinder artefact outside `02-story-creation` Step 4's one permitted edit

### QA Tasks — Automated

- [ ] Extend `shipped-artefacts.sh --self-test` with the seeded-index deletion probe
- [ ] Add the generated-tree grep asserting no syntek-base literal appears in any index
- [ ] Add the `copier update` probe asserting no index is reverted to its seed
- [ ] Run `docs-pairing.sh`, `doc-references.sh` and `docs-length.sh` over the changed tree

### QA Tasks — Manual

- [ ] Row-by-row walk-through of `MAP-INDEX.md` against the map headers as they then stand, recorded
- [ ] Enum derivation walk-through across every map, with the reasoning for each value recorded
- [ ] Re-read of the six repaired instruction sites, recorded
- [ ] Incomplete-index legibility check by a reader who has not read this story

---

## Verification Checks

Run all of the following before raising a PR — via the project scripts under
`code/src/scripts/**/*.sh`, never a raw `python`, `manage.py`, `pytest`, or `docker` call.
All must pass.

- [ ] `bash code/src/scripts/syntax/lint.sh` and `bash code/src/scripts/syntax/check.sh` pass
- [ ] `bash .github/scripts/shipped-artefacts.sh --self-test` passes
- [ ] `bash code/src/scripts/audits/docs-pairing.sh`, `audits/doc-references.sh` and
      `audits/docs-length.sh` pass
- [ ] The `[3/4] Template Generation` job is green on both answer sets
- [ ] No secrets, debug flags, or hardcoded IDs introduced
- [ ] Security acceptance criteria signed off — this story's Security flag is not `N/A`
- [ ] All seven `_tasks` `mv` lines confirmed inside the `copy` gate and ahead of `rmdir .copier`
      in the diff (ST01, ST02)
- [ ] Every `.copier/` index seed confirmed to carry **no** syntek-base instance row (ST03, ST04)
- [ ] The code-review-graph refreshed **after staging**, per `code/docs/CODE-REVIEW-GRAPH.md`

---

## Definition of Done

- [ ] All acceptance criteria met and verified by a reviewer
- [ ] All tasks checked off
- [ ] All verification checks passed
- [ ] Code reviewed and approved (minimum 1 reviewer)
- [ ] No outstanding TODO or FIXME comments introduced by this story
- [ ] `US010` is written into `MAP-REGISTER-INDEXES.md`'s `S-01` and `S-02` Slices rows
- [ ] The new `S-05` row exists with `US011` in its `Story` column
- [ ] `GAPS.md`'s entry of 01/09/2026 is claimed by this story and closed by
      `workflows/22-implementation-documentation/` against the shipped change — never from here
- [ ] Merged to `main` (or the active release branch)
- [ ] Story status updated to **Completed** in the project board
- [ ] Any GDPR or security gaps identified during review are documented in the sprint note
