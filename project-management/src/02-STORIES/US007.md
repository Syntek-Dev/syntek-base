# US007 — The story status vocabulary gets one owner, and every instruction that writes it uses a value the owner admits

**Epic:** Story Lifecycle
**Status:** Open

<!-- FLAGS — one row per gate, and the flag is that gate's entry condition.
     A flag reading N/A means the gate is skipped for this story; every other value means it runs.
     The value is a MANIFEST, not the design: the gate owns the design, may add to this list, and
     the story is updated to match when the gate closes.

     PROVENANCE. Not cut from a slice row, and the absence is a recorded decision rather than an
     omission. project-management/src/01-FEATURE-MAPS/MAP-REGISTER-INDEXES.md:556 files
     "Reconciling the story and story-plan status vocabularies" under what that map will NOT do —
     "Fog of war, not a node — settling it here would make this map the owner of the story
     lifecycle" — and :221-225 parks the plan-template contradiction as "not this map's: it
     belongs to whoever owns the story lifecycle". GAPS.md's entry of 01/09/2026 (:337-359) is
     the gap's only register row.

     Its charting-home sentence (:357-359) suggests MAP-RULE-OWNERSHIP.md on the ground of which
     SURFACES that map's passes covered — not what the map FILE contains, so grepping the map for
     status matches would refute a different proposition and is not attempted. The suggestion is
     UNTESTED and does not need testing: the same sentence says the frontier is closed and the
     gap is recorded on GAPS.md instead, so this story proceeds with no map either way.

     US002 is NOT the precedent for that: its own block records that its no-slice-row premise
     "was measured false on 02/09/2026 — the row exists", so it is precedent for a story a slice
     depends on but does not own, where this story has no map trace at all. The FLAGS below are
     filled from the census of 06/09/2026 and the grilling pass that settled twelve decisions on it.

     EPIC. `Story Lifecycle`, diverging from the one-epic-per-map convention on the record, as
     US002 did. There is no map to name, and the work is the lifecycle a US### moves through: the
     sites it touches sit in planning guides, a skill, workflows, templates and a map, none of
     which is a feature. `Audit Infrastructure` (US002's) would misfile it — nothing here is a
     gate, and the gate that will read the field is US002's line of work.

     Twelve rows are N/A because this story edits Markdown only: no model, no endpoint, no
     screen, no personal-data path, no log line, no public page.

     QA VALUE. doctrine-drift.sh STAYS, narrowed, on ADR-US002-BLIND-GATE-LEAVES-THE-FLAG's
     rule: its SCAN_DIRS (code/src/scripts/audits/doctrine-drift.sh:61-67) include .claude/skills
     and project-management/workflows, so it can open the skill and the workflow files this
     story edits — but :315 logs its scope as "fenced code only", and every claim here is prose
     or a table: it reads part of the files and decides none of the question. skill-conformance.sh
     is added because .claude/skills/CLAUDE.md makes it the definition of done for any skill edit,
     read as an ATTRIBUTED identity diff against a baseline captured immediately before the first
     edit, never as a state quoted from here: a concurrent change to the skills tree moved its
     finding set more than once on 07/09/2026 alone. An empty baseline collapses the criterion to
     exit 0 with nothing attributable to completion/SKILL.md. doc-references.sh takes the same
     treatment, for the reason Scenario 13 gives. -->

| Flag       | Value                                                                                                                                                                    |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| DB         | N/A                                                                                                                                                                      |
| User Flow  | N/A                                                                                                                                                                      |
| Brand      | N/A                                                                                                                                                                      |
| Components | N/A                                                                                                                                                                      |
| Wireframes | N/A                                                                                                                                                                      |
| GDPR       | N/A                                                                                                                                                                      |
| Security   | N/A                                                                                                                                                                      |
| QA         | manual — `docs-length.sh`, `docs-pairing.sh`, `doc-references.sh` (baseline diff), `doctrine-drift.sh` (regression guard only), `skill-conformance.sh` (attributed diff) |
| SEO        | N/A                                                                                                                                                                      |
| API        | N/A                                                                                                                                                                      |
| Logging    | N/A                                                                                                                                                                      |
| Backend    | N/A                                                                                                                                                                      |
| Frontend   | N/A                                                                                                                                                                      |

---

## Client Summary

Every piece of work in this project carries a one-word status — open, blocked, in review,
completed and so on. Today the project's own instructions disagree about which words are allowed:
one list has five, another has eleven, and two of the steps that record progress use words the
five-word list does not permit. Nothing has gone wrong yet, because no work has reached those
states. This story settles the list in one place, has every instruction point at it, and makes
sure the version that ships with each new project is the corrected one — so a recorded status is
always a word the project agreed on.

---

## User Story

As a developer recording where a story stands in this repository, I want the `**Status:**`
vocabulary defined in exactly one document, with every skill, workflow and template that writes
the field drawing its values from there, so that the value I record is one every reader — and the
index gate that will soon string-compare it — agrees is legal.

## MoSCoW Priority

**Must Have**

<!-- Blast radius. US002 unblocks MAP-REGISTER-INDEXES.md slice S-03 (US002.md:108), which
     builds register-indexes.sh and its broken/ + clean/ status fixtures, and that map's :240
     says the gate's Status clause string-equals the row against the file (the verbatim-mirror
     rule is stated at :180 and argued at :190). Whatever vocabulary is canonical on the day that
     gate ships is the one it enforces — and a gate built on a split vocabulary ships the split
     into every generated project with a green tick beside it. Q3 of the grilling pass put this
     story before US002 for that reason alone. -->

## Story Points

5

<!-- Nothing is renamed and zero live source values change — measured, Scenario 12. No index,
     gate or fixture exists yet to repair: register-indexes.sh, STORY-INDEX.md,
     STORY-PLAN-INDEX.md and SPRINT-INDEX.md are all absent on 07/09/2026.

     What earns the 5 is spread and authoring, not difficulty. The Tasks table below is the
     record: twenty distinct files across nine trees, two of the edits new AUTHORED sections
     rather than repointed routes — the per-register scoping declaration in STORIES.md, and a
     "Sprint statuses" section in SPRINTS.md with a meaning against each of three values. The
     completion skill's vocabulary section is rewritten, not trimmed: two enumerations become two
     routes, and it gains a writer table. And STORY-PLAN-US000-TEMPLATE.md — 774 lines on
     07/09/2026, re-measured at implementation, shipped — takes an H2 rename, a row removal,
     a renumber, a partial enumeration corrected at :248, and three clickup-only markings.

     US001's 5 is the closer comparison. Its own estimate comment reads "A new code/docs/ family
     with its pair and index rows, two content migrations, one section reduction, and three
     pointer repairs" — new sections in an owning document plus routes migrated to them, which is
     this shape. US002's 3 is the lower bound: it moved headroom inside ONE register and authored
     no new section. Not an 8 — every edit is decided before the first keystroke, and no file
     needs a design pass. -->

## Dependencies

- **No upstream dependencies.** Wave 0 of the cutting order — it waits on nothing.
- **This story unblocks US002's line of work.** `project-management/src/02-STORIES/US002.md` <!-- doc-references: template-only -->
  lists `MAP-REGISTER-INDEXES.md` slice `S-03` (`register-indexes.sh`) among the registrations it
  unblocks (`:108`), and that gate's `broken/` + `clean/` status fixtures are built against
  whichever set is canonical when they are written. This story ships first so that it is one set.
- **No sprint is named here, and that is the format, not an omission.** A `US###.md` in this
  repository carries no sprint membership — `grep -rn '^\*\*Sprint' project-management/src/02-STORIES/`
  returns nothing on 07/09/2026, and US002 claims none. Sprint membership is recorded in the sprint
  records: `project-management/src/03-SPRINTS/SPRINT-03.md:155-156` <!-- doc-references: template-only -->
  currently sends this story to `SPRINT-04`, and moving it is a re-plan those records own.
- **Not this story's, and whose it is:**

  | Left alone                                                                                                                                                                                                                                                                                | Owner                                                                                                                                                                                                                          |
  | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
  | The Plans Index / `STORY-PLAN-INDEX.md`, and the sites that gate on it — less the two shipped-template sites this story consumes (Scenario 11)                                                                                                                                            | US002's line of work — `MAP-REGISTER-INDEXES.md` slice `S-01` creates the file, `S-03` the gate over it. `S-01` inherits six of the eight sites the map counted, and re-measures the population rather than inheriting a count |
  | `.claude/skills/pm-tool-sync/SKILL.md:59-68` and `:98` — the board-side status mapping and the branch/PR automation over it                                                                                                                                                               | Whoever configures a board — it is the tool-agnostic sync layer, excluded from this story's population on the record (Acceptance Criteria)                                                                                     |
  | Retiring or redrawing `project-management/src/01-FEATURE-MAPS/MAP-REGISTER-INDEXES.md:556` <!-- doc-references: template-only --> (and its stale `:213`, and the graduated fog entry at `:456-469`); `:215-219` is the one map edit this story takes, as the exception Scenario 10 states | A `wayfinder` RESOLVE session — `project-management/src/01-FEATURE-MAPS/CLAUDE.md:15-20` routes all map writes                                                                                                                 |
  | `01-SPRINT-PLAN-01.md:224`'s surviving "Sprint closed on the board" clause                                                                                                                                                                                                                | The sprint's own close under `24-release` — `:226` already carries the repository-side row, so nothing is left unenforced                                                                                                      |
  | `doc-references.sh`'s findings, and any edit to that script                                                                                                                                                                                                                               | `MAP-RULE-OWNERSHIP.md` slice `S-06` (`GAPS.md` 02/09/2026, `:436-437`), carried by US004                                                                                                                                      |
  | The `project-management/src/17-STORY-PLANS/STORY-PLAN-US005-RETRY-OWNERSHIP-AND-BUDGETS.md:9` / `project-management/src/02-STORIES/US005.md:4` status divergence <!-- doc-references: template-only -->                                                                                   | Unexplained, and routed to whoever owns US005's plan — see Scenario 11                                                                                                                                                         |
  | A writer for the sprint value `In Progress`, which Scenario 4 declares writer-less                                                                                                                                                                                                        | The `DEFERRED.md` row the Definition of Done opens, targeted at `03-sprint-planning` and the `sprint` skill, which open the record                                                                                             |
  | A `.copier/migrations/` script <!-- doc-references: template-only -->                                                                                                                                                                                                                     | Nobody yet — `project-management/src/01-FEATURE-MAPS/MAP-REGISTER-INDEXES.md:344` triggers it only once the gate exists, which it does not                                                                                     |

## Decisions

<!-- The ADRs this story rests on. Each is written at the gate that surfaced its trade-off;
     15-decisions checks the set holds and does not clash. Cite by full repo-relative path. -->

- `project-management/src/15-DECISIONS/ADR-US001-INSTANCE-CITATION-UNVERIFIED-02-09-2026.md` <!-- doc-references: template-only --> — no
  gate verifies a PM `src/` instance citation in either form, so every citation in this story is
  human-checked, not machine-checked. Every line number below was re-read on 07/09/2026.
  **`copier.yml`'s line numbers are volatile and must be re-resolved, not trusted:** <!-- doc-references: template-only --> a
  concurrent, uncommitted Wagtail integration moved them more than once while this story was
  being written, so the implementer re-resolves every `copier.yml` line this story cites by its <!-- doc-references: template-only -->
  QUOTED TEXT before believing the number.
- `project-management/src/15-DECISIONS/ADR-US001-PROSE-DOCTRINE-VERIFICATION-02-09-2026.md` <!-- doc-references: template-only --> —
  prose doctrine is verified by a human read-across, and `doctrine-drift.sh` runs as a regression
  guard only; the Acceptance Criteria preamble applies it to every scenario.
- `project-management/src/15-DECISIONS/ADR-US002-BLIND-GATE-LEAVES-THE-FLAG-02-09-2026.md` <!-- doc-references: template-only --> —
  a gate that cannot read the files under test leaves the `QA` manifest; one that reads them but
  cannot decide part of the question stays, narrowed. Applied to `doctrine-drift.sh` in the FLAGS
  comment.
- `project-management/src/15-DECISIONS/ADR-US003-CITATION-GATE-BASELINE-DIFF-02-09-2026.md` <!-- doc-references: template-only --> —
  **binds this story, not only US003.** `doc-references.sh` is read as an identity diff against a
  baseline captured before the first edit. Scenario 13 states it; it does not re-decide it. The
  same treatment is extended to `skill-conformance.sh` under Scenario 13, because a concurrent
  change to the skills tree can move that gate's finding set between the capture and the close.
- **No record is written at this gate.** Each of the twelve decisions the grilling pass settled is
  an edit to a document that can be edited back — which set is canonical, which document owns it,
  which values the skill may write — and none is hard to reverse, which is the test the template's
  Decisions comment applies. The one decision that was hard to make, whether the map owns the
  lifecycle, the map itself settled at `:556`, and this story leaves that row standing.

---

## Acceptance Criteria

One set of eleven story statuses exists, `project-management/docs/planning/STORIES.md` is the only
document that DEFINES it, and every skill, workflow, template and README seed **that writes or
lists a `**Status:**` value into this repository's own artefacts** routes to that section and uses
a value it admits. The sprint set takes the same shape one register over. Nothing is renamed and no
live source value changes — measured, not assumed.

**One surface is excluded from that population, on the record.**
`.claude/skills/pm-tool-sync/SKILL.md` is the tool-agnostic board layer: its mapping at `:59-68`
holds seven strings in none of the eleven (`Backlog`, `To Do`, `Ready`, `Sprint Backlog`, `QA`,
`Done`, `On Hold`), folds `Done` and `Closed` into one `done` bucket, and its `:98` drives a
transition off branch and PR events — the **external board's columns**, not a `**Status:**`
field under `project-management/src/`. It ships unconditionally (`copier.yml:291-293` says so <!-- doc-references: template-only -->
in terms), and reconciling it is a board-configuration decision, named as a non-edit in
Dependencies rather than silently included.

**Every criterion below closes by a recorded human read-across in
`project-management/src/18-TESTS/US007-MANUAL-TESTING.md`**, because no gate in this repository
reads a status value or a doctrine claim expressed in prose or a table:
`code/src/scripts/audits/doctrine-drift.sh:315` logs its scope as "fenced code only", and the only
two extraction sites — `project-management/src/00-ASSETS/scripts/export-clickup-stories.sh:138` and
`sync-clickup.sh:95-103` — accept any string.

**The travel rule is the scoping fact.** `copier.yml:152` <!-- doc-references: template-only --> excludes `/project-management/src/**` and
re-includes only `**/CONTEXT.md` (`:155`), `**/CLAUDE.md` (`:156`), `**/*TEMPLATE*` (`:159`) and a
short allowlist of named files and globs (`:161-181`); `project-management/docs/**` and
`project-management/workflows/**` appear nowhere in `_exclude`, and `.claude/` loses `MEMORY.md`
(`:75`) plus each optional stack-skill tree, gated on its surface. `completion` appears nowhere in
`copier.yml` — `grep -n completion copier.yml` returns nothing on 07/09/2026 — so <!-- doc-references: template-only -->
`completion/SKILL.md` ships unconditionally. So every live instance stays behind and every
template, guide, workflow and skill ships — which means the drift that ships is systematically
the uncorrected one: Scenario 7's `SPRINT-00-TEMPLATE.md:344` is the instance, and the only
version of it that travels is the wrong one.

```gherkin
Scenario: One set of eleven exists, one document defines it, and the skill declares the five it may write
Given project-management/docs/planning/STORIES.md:79-91 defines eleven statuses and :93 calls them
    "the canonical set", while .claude/skills/completion/SKILL.md:37-40 lists five for the same
    header and MAP-REGISTER-INDEXES.md:216 says the skill "owns both"
And the five are a strict subset of the eleven, so nothing is renamed and no value is retired;
    the only question was whether six states exist
When the story ships
Then STORIES.md remains the only document that DEFINES the eleven, with a meaning against each,
    because planning/CONTEXT.md:22 already assigns "statuses" to it and planning/CLAUDE.md:23 —
    the Definition of done, not the concrete-steps bullet at :21-22 — requires a convention to be
    stated in exactly one of the three sub-documents
And .claude/skills/completion/SKILL.md no longer restates the set: its "The status vocabulary"
    section (the story set at :37-40, the sprint set at :42) routes to STORIES.md -> Story
    statuses, using the "owned by ... Not restated here." HTML-comment pattern that
    SPRINT-00-TEMPLATE.md:12-13 already carries, and declares only the five it may write —
    Pending · Open · Blocked · In Review · Completed — keeping the "never straight from Pending or
    Open to Completed" rule, because that rule is the skill's and not the set's
And the declaration names who writes the six it may not (Scenario 4), and says on its face that it
    is a DECLARATION OF AUTHORITY and not a definition — it enumerates eleven values in order to
    assign each an owner, and a reader must not mistake that for a second canonical list
And .copier/README.md:568-570 and STORY-PLAN-US000-TEMPLATE.md:24-30 stop being definition
    sites — each routes to STORIES.md -> Story statuses — while the plan template's header
    pick-list at :9 is left as the field's LEGEND, the shape ADR-US000-TEMPLATE.md:10 uses,
    because a pick-list beside a field is not a definition
And STORY-PLAN-US000-TEMPLATE.md:248 — a partial enumeration in the Dependencies table's Status
    cell, "`Open` / `Pending` / `In Progress` / `Blocked` / `Completed` (ClickUp status)",
    matching neither the eleven nor the five — is routed to STORIES.md -> Story statuses and loses
    its board attribution, for Scenario 6's reason
And project-management/docs/PLANNING-GUIDE.md is NOT made the owner: it self-declares at :16 as
    a thin index holding no rules, and its :22 already routes statuses on to STORIES.md
And that exactly one DEFINITION site remains PER SET — STORIES.md for the eleven, SPRINTS.md's
    new section for the three — is confirmed not by a grep but by a NAMED-SITE INVENTORY in the
    manual-testing record: every site that states a status value, with its ROLE beside it —
    definition (STORIES.md:73-94; SPRINTS.md's new section), pick-list legend
    (STORY-PLAN-US000-TEMPLATE.md:9), writer declaration (completion/SKILL.md), example transition,
    board mapping, order, route, or excluded-by-scope (pm-tool-sync, and the sibling registers of
    Scenario 2). A grep cannot tell a definition from
    a legend or an order from an example, matches :9 which is deliberately left standing, matches
    the skill's writer table, and matches this story file — so the inventory is the test and no
    mechanical check is claimed in its place; Scenario 4 fills the role column for every
    In Progress and Rejected Customer site

Scenario: Status is declared scoped per register, once, in the owning document
Given many sibling registers reuse story-set words with their own meanings, and each declares its
    own set in its own template: reviews (19-REVIEWS/REVIEW-US000-TEMPLATE.md:5, the REVIEW
    record's own field, In Review -> Accepted), findings (20-FINDINGS/FINDING-US000-TEMPLATE.md:5,
    Open -> Triaged), bugs (21-BUGS/BUG-US000-TEMPLATE.md:17, Open / Fixed / Verified), decisions
    (15-DECISIONS/ADR-US000-TEMPLATE.md:10, Proposed | Accepted | Superseded | Deprecated),
    automated tests (18-TESTS/US000-TEST-STATUS.md:5, Green / Red / Partial), manual tests
    (18-TESTS/US000-MANUAL-TESTING.md:5, In progress / Passed / Blocked), refactoring
    (22-REFACTORING/REFACTORING-US000-TEMPLATE.md:19, {Proposed · In Progress · Complete}),
    incidents (23-INCIDENTS/INCIDENT-000-TEMPLATE.md:17, Open / Monitoring / Resolved / Closed),
    vulnerabilities (10-SECURITY/VULNERABILITIES/PLANNING/VULN-PLAN-US000-TEMPLATE.md:17, "Open —
    sprint blocker") and security assessments
    (10-SECURITY/ASSESSMENTS/PLANNING/ASSESSMENT-PLAN-US000-TEMPLATE.md:58, Addressed / Partial /
    Open / N/A)
And that list was found on 07/09/2026 by two sweeps, not one: every *TEMPLATE* under
    project-management/src/, plus the two 18-TESTS records, which carry no TEMPLATE in their
    filename and are re-included by name — copier.yml:177 says why in terms, "The two test
    records are templates named for the story they are copied to, not for their role". Neither
    sweep is a guarantee of completeness
And 19-REVIEWS/REVIEW-US000-TEMPLATE.md carries TWO Status surfaces, which are different fields:
    :5 is the review record's own, while :126-132 records a transition of the STORY register's
    field and is what Scenario 4 refers to
And the register-indexes gate (MAP-REGISTER-INDEXES.md:240) will string-equal a Status column
    per index, so Open meaning two things across two indexes is an ambiguity it cannot detect
When the story ships
Then STORIES.md -> Story statuses states the RULE FIRST: a `Status` value is scoped to the
    register whose template declares it, and the eleven bind US###.md and its story plan and
    nothing else
And the registers above are given as ILLUSTRATIVE and EXPLICITLY NON-EXHAUSTIVE — "for example",
    with the count omitted — because a closed set of siblings goes stale the day a register is
    added, and the rule has to hold for the register nobody has written yet
And no sibling register's SET OF VALUES is edited, merged or renamed by this story: the
    declaration pre-empts the ambiguity, it does not fold the registers into one. Scenario 9's
    touch to 18-TESTS/US000-MANUAL-TESTING.md:5 is a CASING correction inside that register's own
    three-value set and changes no word

Scenario: The sprint set moves to the guide that already names the field
Given .claude/skills/completion/SKILL.md:42 is the only place the sprint set
    (Planned · In progress · Done) is enumerated, while project-management/docs/planning/SPRINTS.md:21
    names status as a required sprint-record field without ever listing its values
And SPRINT-00-TEMPLATE.md:12-13 and the same comment in the four live records (SPRINT-01.md:13-14,
    SPRINT-02.md:16-17, SPRINT-03.md:14-15, SPRINT-04.md:14-15) route the sprint vocabulary to
    the skill
When the story ships
Then SPRINTS.md gains a "Sprint statuses" section holding Planned · In Progress · Done with a
    meaning against each — Scenario 2's per-register rule applied to the sprint register, and
    Scenario 1's single-owner rule applied to the second set
And the skill's sprint line becomes a route to that section plus a declaration that it may write
    Done and nothing else, on the same pattern as its story declaration
And the five ownership comments route to SPRINTS.md -> Sprint statuses instead of the skill, so no
    comment points at a section that no longer states the set — the only edit the four live
    records take from this story
And STORIES.md and SPRINTS.md each stay under the 270 warn tier after what they gain — 97 and 88
    counted lines on 07/09/2026, re-measured at implementation before and after the edit, with
    `bash code/src/scripts/audits/docs-length.sh --path project-management/docs/planning --limit 1`,
    never with wc -l, which reports different figures for both

Scenario: The shipped instructions the skill cannot express are reconciled by naming the writer
Given project-management/workflows/23-pr-and-review/STEPS.md:73-74 orders Accepted /
    Accepted Customer and 24-release/STEPS.md:90 orders Closed, both travel, and the skill that
    RECORDS THE COMPLETION TRANSITIONS (completion/SKILL.md:37-40) can express none of the three
And 23-pr-and-review/STEPS.md:57 orders In Review, which the skill can express — and neither
    workflow's CHECKLIST.md gates any of the four writes its STEPS.md orders
And 23-pr-and-review Step 5's Completed write goes AROUND the skill, not through it: a
    case-sensitive `grep -rn completion project-management/workflows/23-pr-and-review/` returns
    nothing on 07/09/2026, and the case-insensitive form matches only the "## Completion" heading
    at STEPS.md:137, which names no skill — so the skill's "never straight from Pending or Open to
    Completed" guard is real, but binds only where the skill is on the path
And within this repository's own registers nothing ORDERS In Progress or Rejected Customer — a
    distinction a grep cannot make and the named-site inventory of Scenario 1 does. Every
    occurrence of either value across project-management/workflows, .claude/skills and every
    *TEMPLATE* under project-management/src/ (grep -w, 07/09/2026) has a role, and none is an
    order inside this repository: STORY-PLAN-US000-TEMPLATE.md:9 is the legend (both values);
    :25 and :26 sit inside the :24-30 definition comment this story routes; :248 is the partial
    enumeration this story routes; :636 is an EXAMPLE transition in the propagation comment
    ("Open → In Progress → In Review → Completed"); 00-SPRINT-PLAN-00-TEMPLATE.md:90 is the
    column legend Scenario 8 routes; REFACTORING-US000-TEMPLATE.md:19 is a sibling register's own
    set, excluded under Scenario 2; .claude/skills/pm-tool-sync/SKILL.md:64 is the board mapping;
    and :98 is the one order, board-side automation over the external tool's columns, excluded
    from this story's population above
When the story ships
Then the skill's declaration names the writer of each value outside its five: Accepted and
    Accepted Customer by 23-pr-and-review Step 5; Closed by 24-release Step 5; Rejected by the
    review record's transition surface (19-REVIEWS/REVIEW-US000-TEMPLATE.md:130-131, In Review ->
    Rejected — the STORY-register field Scenario 2 scoped, not the review record's own at :5);
    In Progress as written only by the board-side sync, never by an artefact instruction here;
    and Rejected Customer as having no writer in this repository today — declared so, not assigned one
And STORIES.md -> Story statuses carries a one-line pointer at that declaration — who may write
    each value is declared in .claude/skills/completion/SKILL.md — so the owner of the set routes
    to the writer of each value, and the trace of Scenario 6 reaches it from STORIES.md without
    the reader having to know the skill exists
And the sprint register's writers are named on the same pattern: Planned by the template copy —
    SPRINT-00-TEMPLATE.md:10 carries it, written under 03-sprint-planning (STEPS.md:57) by the
    sprint skill's Step 3 (.claude/skills/sprint/SKILL.md:84) — and Done by this skill's
    "Completing a sprint" section (:62-70). In Progress, which this story writes into SPRINTS.md
    with a meaning and nothing in this repository writes into a record, is declared writer-less
    in the same words as Rejected Customer AND handed on: the Definition of Done opens a
    DEFERRED.md row for it, targeted at 03-sprint-planning and the sprint skill, which open the
    record. A sprint that can never legally enter its own middle state is a gap this story names
    rather than one it silently authors
And the :636 example survives Scenario 11's board-neutral rewording as an example, not an order:
    it names values the one set admits, it is listed in the inventory under that role, and the
    rewording changes the comment's attribution, not the example's values
And the three ordering steps are left ordering what they order, because every value they name is
    legal under the one set — the contradiction was the skill's claim, never the workflows'
And 23-pr-and-review/CHECKLIST.md gains a box for the In Review write at Step 3 and the
    Completed / Accepted write at Step 5, and 24-release/CHECKLIST.md gains one for the Closed
    write at Step 5, so a write a STEPS.md orders is a write its CHECKLIST.md checks
And that the shipped instructions no longer contradict the shipped skill is confirmed by a
    read-across of all five files in the manual-testing record, never reported as gate-verified

Scenario: The three stale routes move one hop, and no section is written
Given 02-story-creation/STEPS.md:83, 23-pr-and-review/STEPS.md:59 and 24-release/STEPS.md:92 each
    cite "project-management/docs/PLANNING-GUIDE.md → Story Statuses", that guide has no such
    section, and its :22 routes "statuses" on to planning/STORIES.md, where "## Story statuses"
    sits at :73
And doc-references.sh tests only whether the backticked path resolves and never reads the section
    after the arrow, so all three pass the gate today — stale rather than dangling
When the story ships
Then each of the three cites project-management/docs/planning/STORIES.md -> Story statuses
And no "Story Statuses" section is added to PLANNING-GUIDE.md, which would make a second
    definition site of the index that :16 says holds no rules
And 19-REVIEWS/REVIEW-US000-TEMPLATE.md:131-132, which cites PLANNING-GUIDE.md without naming a
    section, is left as it is — it resolves through :22 in one hop and is not stale
And the three repointed routes are re-read in place by hand — the gate never reads past the arrow —
    and the target section confirmed to state the set, recorded in the manual-testing record

Scenario: The defining sentence is board-neutral, and a project without ClickUp reads the same eleven
Given STORIES.md:75-77 defines the set as "The ClickUp board vocabulary — the template's default PM
    board" and says the values are "pushed by the clickup-sync workflow"
And copier.yml:673-679 makes INCLUDE_CLICKUP an opt-out defaulting to true, and :294-298 removes
    the workflow, the three scripts and export/clickup/** when it is false — while STORIES.md
    travels unconditionally, so a Jira or Linear project reads a vocabulary attributed to a board
    it does not have
And Jinja-conditional prose is ruled out, not merely rejected: how-to/src/TEMPLATE-GUIDE/11-CUSTOMISING.md:113,
    12-EXTENDING.md:239 and copier.yml:183 all state that no file ever has templated contents;
    .github/scripts/check-template-tokens.sh check 3 (:138) fails a paired block delimiter in a
    rendered file, at pre-commit and in CI; and .github/workflows/audit-template.yml:364 diffs two
    generations for byte-identity
When the story ships
Then all eleven values are kept and the defining sentence names no board: the set is this
    repository's story lifecycle and a board is mapped onto it — the sentence at :93-94 already
    says "if a project's board uses different words, map them here once", and it is now true of
    ClickUp as well
And the export and push are mentioned only as consumers of the set, carrying the bold clickup-only
    marker that project-management/CONTEXT.md:89 and .copier/README.md:591 already use for the
    same boundary
And .copier/README.md:568-570 — moved to README.md at generation by copier.yml:958 — stops
    attributing the values to ClickUp and routes to STORIES.md. The lines sit under the H3 at
    :566, "ClickUp story sync — client-facing exports", whose push paragraph at :591 already
    carries the bold clickup-only marker; the heading itself is left as it is
And a reader of a project generated with INCLUDE_CLICKUP: false, tracing STORIES.md and README.md,
    reaches the eleven values, the per-register rule and — through the pointer Scenario 4 adds —
    the writer of each value, none attributed to a board the project lacks and no instruction on
    the path naming a removed script; confirmed by a read-across against the :294-298 exclusion
    list, re-resolved first, and recorded in the manual-testing record

Scenario: The sprint template ships Completed, and gains the row every live sprint already carries
Given SPRINT-00-TEMPLATE.md:344 ships "marked **Done**", a value in no story set, and all four live
    sprints corrected it to Completed (SPRINT-01.md:172, SPRINT-02.md:284, SPRINT-03.md:329,
    SPRINT-04.md:338)
And the template's Definition of Done lacks the row all four live sprints carry — "Sprint
    **Status:** set to Done" (SPRINT-01.md:178, SPRINT-02.md:294, SPRINT-03.md:340,
    SPRINT-04.md:351) — and reads "Sprint closed in the project board" at :350 in its place
And 00-SPRINT-PLAN-00-TEMPLATE.md:170 ships the same clause as "Sprint closed on the board", which
    01-SPRINT-PLAN-01.md did NOT rewrite — it still carries that wording verbatim at :224 and ADDED
    a Status row beside it at :226 — while 02-SPRINT-PLAN-02.md:312 and 03-SPRINT-PLAN-03.md:371
    carry a Status row and no board string at all
When the story ships
Then SPRINT-00-TEMPLATE.md:344 reads Completed
And its Definition of Done carries "Sprint **Status:** set to Done" in place of the board clause at
    :350 — board-neutral for Scenario 6's reason, and in the wording the four live sprints already
    share, so the fifth sprint copies a row rather than reinventing one
And 00-SPRINT-PLAN-00-TEMPLATE.md:170 takes the wording 02-SPRINT-PLAN-02.md:312 uses — the sprint
    record's Status set to Done — so a fourth plan inherits it rather than a fourth phrasing
And 01-SPRINT-PLAN-01.md:224's surviving board clause is left standing, named in Dependencies
    with its owner: a live plan's own DoD whose :226 already carries the repository-side row, and
    this story's board-neutrality work is scoped to what TRAVELS
And the four live sprint records and the three live plans take no other edit for this

Scenario: The sprint-plan Status column is ratified onto the eleven, and the false rows are corrected
Given 00-SPRINT-PLAN-00-TEMPLATE.md:90 ships {Draft / Ready / In Progress / Done} as the legend of
    the story-plan Status column — a fourth vocabulary — and zero of those four values appears in
    any live plan
And 01-SPRINT-PLAN-01.md:109-110 and 02-SPRINT-PLAN-02.md:178 carry "Not started", a value in no
    set anywhere, and false against the plans they point at: the five live story plans carry
    Open x4 and Blocked x1 at line 9 each
And 03-SPRINT-PLAN-03.md:174-175 already mirrors the story-plan vocabulary and argues at :177-182
    that the difference BETWEEN ITS TWO PLAN ROWS — US005 Blocked, US003 Open — is deliberate
When the story ships
Then the legend at :90 routes to STORIES.md -> Story statuses — the column mirrors the story plan's
    Status field verbatim, which is 03-SPRINT-PLAN-03's position ratified
And the three "Not started" cells read Open, the value each named plan carries at its line 9
And no story plan's own Status field is edited — the column mirrors, it does not lead; the three
    cells are the live mirror cells Scenario 12 counts as the only live cells that change

Scenario: The casing is normalised to In Progress
Given "In progress" appears at three sites outside the two files this story writes about the
    vocabulary — completion/SKILL.md:42, MAP-REGISTER-INDEXES.md:217 quoting it, and
    18-TESTS/US000-MANUAL-TESTING.md:5 — while "In Progress" is the form everywhere else,
    including .claude/skills/pm-tool-sync/SKILL.md:64 (grep -w over .md, .sh and .yml, 07/09/2026)
When the story ships
Then the sprint set is written In Progress wherever this story writes it — SPRINTS.md, the skill's
    route and the corrected map clause — so the first two sites are gone as a consequence of
    Scenarios 3 and 10 rather than as an extra edit
And US000-MANUAL-TESTING.md:5 is normalised to In Progress as a casing touch only: the
    manual-testing register keeps its own three-value set under Scenario 2, and no word changes
And a closing `grep -rnw "In progress"` returns zero over a POPULATION STATED IN THIS CRITERION:
    .md, .sh and .yml, excluding handoffs/ and the two files whose subject is the casing and
    which must quote both forms — project-management/src/02-STORIES/US007.md and
    project-management/src/18-TESTS/US007-MANUAL-TESTING.md — without which the grep can never
    return zero. The command and its exclusions are recorded verbatim in the manual-testing record

Scenario: The map's falsified assertion is corrected, and its other rows are not retired
Given MAP-REGISTER-INDEXES.md:215-219 asserts that completion/SKILL.md:37-42 "owns both"
    vocabularies — the clause this story falsifies: STORIES.md owns the story set, SPRINTS.md the
    sprint set, and the skill owns neither
And :456-469 carries a SECOND assertion this story also falsifies — "the eleven-state ClickUp
    vocabulary defines the story's own **Status:** header on four surfaces", the plan template,
    STORIES.md:79-93, .copier/README.md:568-570 and 23-pr-and-review/STEPS.md:73-74 — but it is
    already struck through and reads "graduated off this map": a dated record closed in place, not
    a live claim, whose onward live claim is the GAPS.md entry this story closes at its Definition
    of Done
And :556 records the reconciliation as "Fog of war, not a node — settling it here would make this
    map the owner of the story lifecycle", and :213 asserts SPRINT-00-TEMPLATE.md has no Status
    field, which that template's :10 carries today
And project-management/src/01-FEATURE-MAPS/CLAUDE.md:15-17 routes every map write through
    wayfinder — "CHART writes the map; RESOLVE updates it one node per session" — and :18-20
    reserves Opus for "moving a resolved row, fixing a link, or updating the index"; rewording a
    prose clause is none of those, and the map's own log at :571 records that Batch C closed it,
    so no RESOLVE session is scheduled to come back to it
When the story ships
Then :215-219 is corrected to the fact — one story set owned by STORIES.md, of which the skill
    writes five; one sprint set owned by SPRINTS.md, of which it writes one — as a stated
    EXCEPTION to that folder's routing rule, not under a licence it grants: a false ownership
    assertion on a live map row must not survive the change that falsifies it, and the map is
    closed, so the alternative is a false claim standing until a session nothing schedules. The
    exception is recorded as such in the manual-testing record, with this reason
And :456-469 takes no edit — editing it would rewrite a closed record
And :556 is NOT retired, redrawn or annotated as closed: the row records a refusal to own, the
    refusal was correct, and redrawing a frontier is a wayfinder RESOLVE session's job
And :213 is likewise left to that session — stale for a reason unrelated to this story, and a
    second exception this story does not take
And the one edit and the three non-edits are recorded with their reasons in the manual-testing record

Scenario: The propagation table names only artefacts that exist, and its ClickUp rows are marked
Given STORY-PLAN-US000-TEMPLATE.md:633 heads the section "Status Propagation & ClickUp Sync" and
    :635-640 describe every transition as mirrored onto the ClickUp board
And row 3 at :646 points at a "Plans Index" in 17-STORY-PLANS/CONTEXT.md that does not exist —
    `rg -i index` on that 64-line file returns zero on 07/09/2026 — and :773 requires the status
    to agree "across story + plan + Plans Index + sprint/sprint-plan"
And the ClickUp export row (row 6 as shipped, :649) and the regenerate block at :653-658 name
    export-clickup-stories.sh, which copier.yml:295 removes when INCLUDE_CLICKUP is false, in a
    template that ships unconditionally
And the rule is already broken in a live pair — STORY-PLAN-US005-RETRY-OWNERSHIP-AND-BUDGETS.md:9
    holds Blocked while project-management/src/02-STORIES/US005.md:4 holds Open — and no artefact
    explains it: 03-SPRINT-PLAN-03.md:177-182 argues only that its two PLAN rows differ
    deliberately, never mentions US005.md, and makes no claim about plan-versus-story
When the story ships
Then the H2 and its comment are renamed and reworded board-neutrally: status propagation across
    the repository's own artefacts, with the board sync a clickup-only consumer of row 1
And row 3 is removed and the rows below it renumbered, because a shipped template can name
    neither a file that does not exist nor the per-project story that will create it; one line
    under the table says a status index over the plans joins the table when the register-indexes
    work lands, and the hand-off is recorded in Dependencies and by 22-implementation-documentation
And the rationale is stated NARROWLY: row 3 is removed because it is a STATUS-PROPAGATION target,
    which is this story's register. The shipped template holds four Plans Index sites — :16,
    :625, :646 and :773 — and this story consumes two, :646 removed and :773 rewritten. The other
    two, and every instruction site outside the template — 17-story-plans/CHECKLIST.md:82,
    STEPS.md:131, CONTEXT.md:47, CLAUDE.md:23 and :30, and src/17-STORY-PLANS/CLAUDE.md:26 on
    07/09/2026 — are
    index-creation instructions, which MAP-REGISTER-INDEXES.md claims for slice S-01 at :72, :212
    and :392, and are left standing whatever column they mention. Of the eight sites the map
    counted, six survive this story; this sweep found ten in all, so eight stand after it, and
    S-01 starts from the list above rather than from either count
And the ClickUp export row and the regenerate block carry the bold clickup-only marker, so a
    project without the scripts reads a row it may skip rather than a command it cannot run
And :773 drops "Plans Index" and names the value as the final canonical value rather than the
    final ClickUp value, with its export-regeneration clause marked clickup-only
And the US005 divergence is recorded in the manual-testing record as pre-existing, UNEXPLAINED by
    any artefact, and routed to whoever owns US005's plan — named, and given an owner

Scenario: Zero live source values change, and that is measured, not assumed
Given a FIELD-SCOPED git history search for each of the six values outside the skill's five —
    `git log --all -S'**Status:** <value>' -- project-management/src/02-STORIES/
    project-management/src/03-SPRINTS/` and ``git log --all -S'| Status | `<value>`' --
    project-management/src/17-STORY-PLANS/`` — returns 0 commits for all six (07/09/2026), so no
    live artefact has ever held one
And the bare `git log --all -S"<value>"` form is NOT used: it cannot tell a **Status:** field from
    the same word in prose, and over the same three folders it returns commits for all six
And on 07/09/2026 the seven live stories all hold Open (US001 to US007, each at line 4), the four
    live sprint records all hold Planned, and the five live story plans hold Open x4 and Blocked
    x1 — sixteen source fields, of which one is this story's own and is expected to move to
    Completed at its Definition of Done. The population is re-counted immediately before the
    first edit, and a story, sprint or plan added between now and then joins it
When the story ships
Then every source field but this story's own holds the value it held before the first edit, this
    story's own moves only at its DoD, and the before/after list of the whole population is
    recorded in the manual-testing record
And the only live cells that change are the three sprint-plan mirror cells of Scenario 8, which
    held a value outside every set and now hold the value their source field carries
And the record states the search, its population and its exclusions — three registers, the
    counted fields, the sibling registers excluded under Scenario 2 and pm-tool-sync excluded
    above — so "zero" is a measurement with a scope and not a claim

Scenario: The citation gate and the skill gate are read as identity diffs against a captured baseline
Given ADR-US003-CITATION-GATE-BASELINE-DIFF binds every story in this backlog until the gate is
    green, and doc-references.sh reports findings in three named classes — [dangling path],
    [instance citation] and [template-only citation] — most of the first being forward references
    to guides US001, US003 and US005 deliver, owned by those deliveries and not by a gate fix. No
    count is quoted: the set moves with every story that lands, and a stale figure is worse than none
And GAPS.md's entry of 02/09/2026 (:387-445) documents those citation classes and hands the first
    edit to doc-references.sh to MAP-RULE-OWNERSHIP.md slice S-06 (:436-437), which US004 carries
    (US004.md:11-12)
And skill-conformance.sh's finding set on this tree moves with a concurrent Wagtail integration —
    measured red twice and green once in a single afternoon on 07/09/2026 — so its state on any
    given day is not quoted either: the baseline is whatever the gate reports immediately before
    the first edit, and it may legitimately be empty
When the story ships
Then both gates' finding sets — their identities, not merely their counts — were captured
    immediately before the first edit and recorded in
    project-management/src/18-TESTS/US007-MANUAL-TESTING.md, an empty set recorded as empty
And no NEW unresolved citation appears from any file this story writes or edits, read as a diff
    against that baseline by identity, because a count nets to zero when one finding clears as
    another appears
And skill-conformance.sh reports NO NEW VIOLATION ATTRIBUTABLE TO .claude/skills/completion/SKILL.md,
    read the same way — a finding on any other skill is a concurrent change's, and is neither
    cleared, inherited nor reported as this story's; where the baseline was empty and the gate
    exits 0 at close, that IS the pass, and is reported as one
And neither gate is reported as passing while a non-empty baseline stands, and nothing in
    doc-references.sh is edited — its fix is S-06's, carried by US004
```

### QA Acceptance Criteria — Manual

<!-- Closed by `project-management/src/18-TESTS/US007-MANUAL-TESTING.md`. -->

- [ ] `bash code/src/scripts/audits/docs-length.sh --path project-management/docs/planning --limit 1`
      and `--path .claude/skills/completion --limit 1` — `STORIES.md`, `SPRINTS.md` and the skill
      all still under 270 counted lines after what they gain (97, 88 and 77 when measured on
      07/09/2026; re-measured at implementation); measured by the gate, never by `wc -l`
- [ ] `bash code/src/scripts/audits/docs-pairing.sh` — read as an identity diff against a
      baseline captured immediately before the first edit, never as a bare exit code: it walks the
      whole tree, so another story landing can move it. It decides nothing here either way — this
      story creates, splits and moves no pair
- [ ] `bash code/src/scripts/audits/doc-references.sh` — no **new** finding against the baseline
      captured before the first edit, by identity; never reported as passing while a non-empty
      baseline stands
- [ ] `bash code/src/scripts/audits/doctrine-drift.sh` — no **new** drift. It reads fenced code
      only, so it cannot see any of the thirteen scenarios; it runs as a regression guard
      (`ADR-US001-PROSE-DOCTRINE-VERIFICATION`) and is never reported as though it had looked
- [ ] `bash code/src/scripts/audits/skill-conformance.sh` — no **new** violation attributable to
      `.claude/skills/completion/SKILL.md`, read as an identity diff against the baseline captured
      before the first edit; an empty baseline with exit 0 at close is a pass and is reported as
      one, and a non-empty baseline is reported as a diff
- [ ] A human read-across of every route this story writes — the skill's two, the three
      repointed, the five ownership comments, the README seed's, the plan template's three
      (`:9` legend confirmed left, `:24-30`, `:248`), the sprint-plan template's, and the
      `STORIES.md` pointer at the skill's writer declaration — lands on a section that states the
      set it names, or, for the pointer, on the declaration itself
- [ ] A cold reader of `.claude/skills/completion/SKILL.md` can name, without leaving the file,
      the five values it writes, the one sprint value it writes, and who writes the other six —
      and can tell that the writer table is a declaration of authority, not a second definition
- [ ] A cold reader tracing `STORIES.md` and `README.md` as generated with
      `INCLUDE_CLICKUP: false` meets no instruction that names a removed script, and finds no
      value attributed to a board
- [ ] The named-site inventory of Scenario 1 with its role column, the before/after list of the
      source fields, the three mirror cells and the closing `In progress` grep with its stated
      exclusions are all recorded with their dates
- [ ] A tester other than the author has signed the walk-through off

---

## Tasks

All tasks below map directly to an acceptance criterion above. Mark each complete before raising a PR.

### Documentation Tasks

| Task                                                                                                                                                                                                                                                               | Done |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---- |
| Capture the `doc-references.sh` and `skill-conformance.sh` finding sets by identity, and every live source status value, **before** any edit — an empty set recorded as empty                                                                                      | [ ]  |
| `STORIES.md` — board-neutral defining sentence; per-register scoping declaration, rule first and registers illustrative; one-line pointer at `completion/SKILL.md`'s writer declaration; clickup-only marker on the export mention                                 | [ ]  |
| `SPRINTS.md` — add the "Sprint statuses" section, `Planned · In Progress · Done`, one meaning each                                                                                                                                                                 | [ ]  |
| `completion/SKILL.md` — replace both enumerations with routes; declare the five and `Done`; name the writer of each other value, story and sprint, with `Rejected Customer` and the sprint `In Progress` declared writer-less; say it is authority, not definition | [ ]  |
| Repoint the three stale `PLANNING-GUIDE.md → Story Statuses` routes to `planning/STORIES.md` -> Story statuses                                                                                                                                                     | [ ]  |
| `23-pr-and-review/CHECKLIST.md` and `24-release/CHECKLIST.md` — add the boxes for the writes their `STEPS.md` order                                                                                                                                                | [ ]  |
| `SPRINT-00-TEMPLATE.md` — `:344` to `Completed`; the DoD row in place of `:350`; ownership comment to `SPRINTS.md`                                                                                                                                                 | [ ]  |
| The four live sprint records — ownership comment to `SPRINTS.md` -> Sprint statuses, nothing else                                                                                                                                                                  | [ ]  |
| `00-SPRINT-PLAN-00-TEMPLATE.md` — `:90` legend to a route; `:170` to the sprint-record `Status` wording                                                                                                                                                            | [ ]  |
| `01-SPRINT-PLAN-01.md:109-110` and `02-SPRINT-PLAN-02.md:178` — `Not started` to `Open`; leave `:224`'s board clause standing                                                                                                                                      | [ ]  |
| `STORY-PLAN-US000-TEMPLATE.md` — `:24-30` route; `:248` route and drop the board attribution; `:633-658` rename and reword (the `:636` example kept as an example), drop row 3, renumber, mark the ClickUp export row and the regenerate block; `:773`             | [ ]  |
| `.copier/README.md:568-570` — drop the board attribution; route to `STORIES.md`; leave the `:566` heading                                                                                                                                                          | [ ]  |
| `project-management/src/01-FEATURE-MAPS/MAP-REGISTER-INDEXES.md:215-219` <!-- doc-references: template-only --> — correct the ownership clause, as the stated exception; leave `:213`, `:456-469` and `:556` standing                                              | [ ]  |
| `US000-MANUAL-TESTING.md:5` — `In progress` to `In Progress`, casing only                                                                                                                                                                                          | [ ]  |
| Re-run the five gates; build the Scenario 1 named-site inventory with its role column; run the casing grep with its stated exclusions; record every figure with its date                                                                                           | [ ]  |

### QA Tasks — Manual

- [ ] Run the five gates named in the QA flag and record the output in
      `project-management/src/18-TESTS/US007-MANUAL-TESTING.md`, recording `doc-references.sh` and
      `skill-conformance.sh` against their captured baselines by identity — an empty baseline
      recorded as empty — rather than against a state quoted from this story
- [ ] Read each route written by this story in place and confirm the target section states the set
- [ ] Trace the `INCLUDE_CLICKUP: false` reading against `copier.yml:294-298` <!-- doc-references: template-only -->
      and confirm no removed script is named on the path, re-resolving the line numbers first
- [ ] Record the named-site inventory with its role column, the source fields before and after,
      the three mirror cells and the closing casing grep with its population and exclusions
- [ ] Record the US005 plan-versus-story status divergence as pre-existing, unexplained by any
      artefact, and routed to whoever owns US005's plan

---

## Verification Checks

Run before raising a PR — via the project scripts under `code/src/scripts/**/*.sh`, never a raw
`python`, `manage.py`, `pytest`, or `docker` call.

<!-- The code-path checks below are marked N/A with a reason rather than deleted: this story ships
     documentation only, and per code/docs/GATE-REPORTING.md a skip is never reported as a pass. -->

- [ ] `bash code/src/scripts/audits/docs-length.sh` passes
- [ ] `bash code/src/scripts/audits/docs-pairing.sh` passes
- [ ] `bash code/src/scripts/audits/doc-references.sh` — no **new** finding against the baseline
      captured before the first edit, by identity rather than count
      (`ADR-US003-CITATION-GATE-BASELINE-DIFF`); never a pass while a non-empty baseline stands
- [ ] `bash code/src/scripts/audits/doctrine-drift.sh` — no **new** finding; a regression guard
      only, because it reads fenced code and every claim this story reconciles is prose or a table
      (`ADR-US001-PROSE-DOCTRINE-VERIFICATION`, `ADR-US002-BLIND-GATE-LEAVES-THE-FLAG`)
- [ ] `bash code/src/scripts/audits/skill-conformance.sh` — no **new** violation attributable to
      `.claude/skills/completion/SKILL.md`, read as an identity diff against the baseline captured
      before the first edit; a plain pass is reported as one only where that baseline was empty
- [ ] `bash code/src/scripts/syntax/lint.sh` passes (Markdown lint and formatting) — this is the
      leg that reads Markdown, via markdownlint-cli2
- [ ] `bash code/src/scripts/syntax/check.sh` — **N/A**, it type-checks Python, TypeScript and
      Rust and has no Markdown leg; this story ships Markdown only, so it has nothing to look at
- [ ] Template, django-component, and HTMX-partial tests — **N/A**, this story adds and edits no
      template, component or partial
- [ ] `migrate.sh check` — **N/A**, this story touches no model
- [ ] `tests/all.sh --coverage` — **N/A**, this story ships no code path
- [ ] Accessibility (WCAG 2.2 AA) — **N/A**, this story renders no interactive surface; it ships
      Markdown guides, workflow steps and templates read in an editor
- [ ] No secrets, debug flags, or hardcoded IDs introduced
- [ ] GDPR / Logging / Security / SEO criteria — **N/A**, each flag reads `N/A`

---

## Definition of Done

- [ ] All acceptance criteria met and verified by a reviewer
- [ ] All tasks checked off
- [ ] All verification checks passed
- [ ] Code reviewed and approved (minimum 1 reviewer)
- [ ] No outstanding TODO or FIXME comments introduced by this story
- [ ] Merged to `main` (or the active release branch)
- [ ] Story status updated to **Completed** — in this file's own `**Status:**` header, the source
      every mirror reads, and the one field of Scenario 12's population expected to move
- [ ] `02-story-creation` Step 4's map back-fill recorded as **N/A** with its reason — no slice row
      exists, by `MAP-REGISTER-INDEXES.md:556`'s decision — rather than skipped <!-- doc-references: template-only -->
- [ ] `GAPS.md`'s entry of 01/09/2026 closed by `22-implementation-documentation`, with its
      `MAP-RULE-OWNERSHIP.md` charting-home suggestion recorded as **untested and not required**,
      for the reason the FLAGS provenance gives
- [ ] `DEFERRED.md` gains two rows, written by `22-implementation-documentation`: the
      propagation-table row 3 hand-off — the Plans Index and `STORY-PLAN-INDEX.md`, with US002's
      line of work as its named target, naming the two shipped-template sites this story consumed
      so that `S-01` re-measures its population rather than inheriting the map's count
      (Scenario 11) — and the sprint value `In Progress`, which `SPRINTS.md` now defines and
      nothing writes, targeted at `03-sprint-planning` and the `sprint` skill (Scenario 4)
