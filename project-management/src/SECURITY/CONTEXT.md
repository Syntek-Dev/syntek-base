# project-management/src/SECURITY

Security audits, threat models, and assessments.

## Directory Tree

```text
project-management/src/SECURITY/
├── CONTEXT.md               ← this file
├── AUDITS/                  ← periodic security audit summaries (created as needed)
│   └── AUDIT-<SCOPE>-DD-MM-YYYY.md
├── THREAT-MODEL/            ← STRIDE analysis documents (created as needed)
├── ASSESSMENTS/             ← security posture assessments (created as needed)
└── VULNERABILITIES/         ← vulnerability review files (created as needed)
```

Subdirectories created as needed:

- `AUDITS/` — periodic security audit summaries
- `THREAT-MODEL/` — STRIDE analysis documents
- `ASSESSMENTS/` — security posture assessments
- `VULNERABILITIES/` — vulnerability review files

**Naming:** `AUDIT-<SCOPE>-DD-MM-YYYY.md`
