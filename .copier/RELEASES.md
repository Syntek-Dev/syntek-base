# Releases — <%PROJECT_NAME%>

**Last Updated**: <%DATE%> **Version**: 0.1.0 **Maintained By**: <%ORG_NAME%>
**Language**: British English (en_GB)

User-facing release notes for each published version.

Where `CHANGELOG.md` records _what changed_, this records _what it means for whoever uses
<%PROJECT_NAME%>_ — written for a reader, not a diff.

---

## v0.1.0 — <%DATE%>

**Status:** Initial scaffold. Nothing is built yet.

Generated from the base template. The stack, the documentation layers and the workflows are in
place; the application is not. Feature work starts by charting one in
`project-management/workflows/01-feature/`.

Before that, two things run once (`how-to/workflows/01-first-time-setup/` Steps 7–8): sharpen the
project brief in `CONTEXT.md`, then run `/scale-planning` to settle what this project is sized for
and therefore what it does not need.

<!--
Your release notes start here. Add a new section above this comment per release, newest first.

Seeded once at generation; `copier update` never touches this file.
-->
