# Changelog

**Last Updated**: <%DATE%> **Version**: 0.1.0 **Maintained By**: <%ORG_NAME%>
**Language**: British English (en_GB)

All notable changes to <%PROJECT_NAME%> are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

Rules and the files each bump touches: `project-management/docs/VERSIONING-GUIDE.md`.

---

## [0.1.0] - <%DATE%>

### Added

- Generated from the base template: the Django monolith, the three documentation layers
  (`code/`, `how-to/`, `project-management/`), the Claude Code agent and skill surface, the
  Docker Compose stack, and the CI workflows.

<!--
Your history starts here. Add a new section above this comment on every version bump,
newest first.

This file is seeded once at generation and is yours from that moment — `copier update`
never touches it, so the template's own history can never overwrite yours.
-->
