# Claude Code runtime
.claude/scheduled_tasks.lock

# Environment
.env
.env.*
!.env.example
!.env.*.example

# Python / Django
.venv/
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.mypy_cache/
.ruff_cache/

# Django runtime
*.sqlite3
staticfiles/
mediafiles/

# Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
.npm/
.cache/

# Generated output (none at present — the stack has no codegen step)
code/src/frontend/src/graphql/generated/*.ts

# Machine spec — host-specific, auto-generated by install.sh (bash install.sh --spec)
code/docs/MACHINE-SPEC.md

# Test coverage
coverage/
.nyc_output/

# Generated test reports
code/src/scripts/tests/reports/

# Docker
*.override.yml

# OS
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# IDE
.vscode/
.idea/
*.iml

# Celery beat PersistentScheduler state (runtime DB — redirected to /tmp in compose;
# this guards against a stray run writing it into the bind-mounted repo working tree)
celerybeat-schedule
celerybeat-schedule*

# Claude Code agent worktrees (nested git repos — never commit)
.claude/worktrees/
.claude/token.md

# Added by code-review-graph
.code-review-graph/

# code-review-graph install generates AI-assistant rule files at the repo root. This project is
# Claude Code only (its manual is .claude/CLAUDE.md), so keep the generated duplicates out of the
# repo — they regenerate on `code-review-graph install`. Leading slashes anchor to root, so
# .claude/CLAUDE.md, code/CLAUDE.md, etc. are unaffected.
/CLAUDE.md
/AGENTS.md
/GEMINI.md
/.cursorrules
/.windsurfrules
/.kiro/

# Keep Claude Plugins
!.claude/plugins/
